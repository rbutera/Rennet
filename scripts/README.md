# Scripts

Repository-owned boundary checks, state-keying checks, hook installation, fixtures, and maintenance commands belong here. A script is not a gate until its verification has a calibrated failing control.
