// Expo Router's entry: it registers the root component that mounts the `app/`
// route tree. Kept as the package `main` (apps/mobile/package.json). No app logic
// lives here — routes are files under `app/`.
import "expo-router/entry";
