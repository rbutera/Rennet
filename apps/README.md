# Applications

Production application entry points belong here. The planned first package is `desktop`, the Electron host. A future `mobile-placeholder` package exists only when its boundary gate is ready. Use the Electron, Vite, Forge, UI, and release choices in [Rennet Dependency Standard](../docs/Rennet%20Dependency%20Standard.md).
