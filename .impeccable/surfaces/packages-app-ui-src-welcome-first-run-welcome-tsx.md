---
version: 1
slug: "packages-app-ui-src-welcome-first-run-welcome-tsx"
primary_target: "packages/app-ui/src/welcome/first-run-welcome.tsx"
related_targets: ["packages/app-ui/src/welcome/welcome.css", "packages/app-ui/src/welcome/welcome-constellation.tsx", "packages/theme/src/constellation-models.ts"]
---

# First-run welcome

- Scope: the five-step welcome, including replay; visual authority: `packages/app-ui/DESIGN.md` with the root working-app rules preserved outside this surface.
- Job: choose appearance, inspect available tools, choose review setup, review optional access guidance, and start a chat. Start reveals Appearance after the opening.
- Direction: adapt the marketing constellation world into a persistent full-screen scene behind theme-controlled glass setup panels. The main app shares its gradient/glass material; particles remain welcome-only.
- Opening: many floating sampled code-glyph blocks in Rennet colors surround “You stopped writing the code. You still have to answer for it.” and Start. Start coalesces points into the Rennet sphere and reveals the appearance panel. This records the corrected implementation, not visual approval.
- Appearance copy: H1 “Welcome to your new Review Harness.” with “Review Harness.” italic; beneath it, “Rennet makes <subject> <quality>” cycles 18 subjects and 12 qualities independently at 3100ms and 4300ms. Screen readers receive one stable sentence.
- Sequence after Start: Appearance sphere, Tools spanner, Review setup gavel, Access connected machines, Ready sphere. One canvas stays mounted outside all step pages; shared geometry lives in `packages/theme/src/constellation-models.ts`.
- Reading material: 78% theme surface with 18px blur and a thin inset highlight, without a broad panel shadow. Panel fill and controls follow the selected app theme and light/dark preference. The background, header, caption, particles, and live/static mark follow the selected theme and mode. Light mode inverts the full reading field. Paired theme swatches represent their displayed mode, including GitHub. The macOS header reserves 108px for traffic lights.
- Hierarchy: a larger opening display headline, separate arrival title and refrain, and Geist setup headings at 32–48px and a 24px panel radius adapt the marketing proportions to setup. Mobile uses 32px headings and a 20px radius; neither expands the working-app ramp.
- Interaction: motion is automatic with no motion buttons. OS reduced motion stops ambient motion and word cycling and makes scene changes immediate. Static branding replaces unavailable WebGL without removing setup controls. Geometry is decorative, not a progress indicator.
- Responsive rule: retain the full-screen backdrop on every viewport; stack foreground content at 760px and below with space above it for the geometry. Appearance previews use two columns.
- Access and exit: Full Disk Access is optional on macOS; without it the OS may prevent reading external, network, or protected locations. Continue never depends on granting it. No project picker or project summary belongs in welcome. Ready completes setup and opens generic New Chat.
- Evidence: `.impeccable/verification/welcome-*.png` includes captures across all five themes in light and dark modes. `app-new-chat-github-dark.png` and `app-settings-github-{light,dark}.png` capture the corresponding real-app material. These are implementation evidence, not a visual approval claim.
