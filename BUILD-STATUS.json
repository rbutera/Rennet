{
  "a1": {
    "status": "done",
    "passes": true
  },
  "a2": {
    "status": "done",
    "passes": true
  },
  "a3": {
    "status": "done",
    "passes": true
  },
  "a4": {
    "status": "done",
    "passes": true
  },
  "a5": {
    "status": "done",
    "passes": true
  },
  "a6": {
    "status": "done",
    "passes": true
  },
  "a7": {
    "status": "done",
    "passes": true
  },
  "b01": {
    "status": "done",
    "passes": true
  },
  "b02": {
    "status": "done",
    "passes": true
  },
  "b03": {
    "status": "done",
    "passes": true
  },
  "b04": {
    "status": "done",
    "passes": true
  },
  "b05": {
    "status": "done",
    "passes": true
  },
  "b06": {
    "status": "done",
    "passes": true
  },
  "b07": {
    "status": "done",
    "passes": true
  },
  "b08": {
    "status": "done",
    "passes": true,
    "note": "PRs #521+#526 landed, 8 sub-wave-2 review fixes confirmed; composition-root wiring is B09's consuming turn (docs framed planned)"
  },
  "b09": {
    "status": "done",
    "passes": true,
    "note": "PR #531 landed; dual review upheld 8 findings (2 critical: durable restart idempotency, turn-loop lost-update), all fixed + confirmed. createRoundsRuntime is runLensPipeline's consuming turn; create-server/dispatch wiring deliberately deferred to the C9/B10 round trigger (5.1/6.2 ledger deferrals). Unblocks: B10 cluster 6, B11, C03/C07/C12 rename seams, C09 live rounds"
  },
  "b10": {
    "status": "done",
    "passes": true,
    "note": "clusters 1-5 landed (PR #523, 11 review fixes confirmed); cluster 6 live-turn wiring B9-gated"
  },
  "b11": {
    "status": "done",
    "passes": true,
    "note": "PR #537 landed (squash 5caaf247). Deep dual review: opus REQUEST-CHANGES + Codex BLOCK w/ adversarial probes \u2192 10 findings (2 P0 + 6 P1 + 2 P2) all fixed + confirmed by fresh opus (CAS positive control proven red by mutation). Durable append-only ask log (sole write path, receipt-is-undo, corrupt\u2192refuse not silent-empty, fsync), body+line two-strata compose (partition, LEFT-side restored), round.dispatch deterministic-key coalesce + own-branch-only routing (viewerDidAuthor), CAS+splice span rework. 5.1 ask-log architecture SOUND. Ledgered follow-ups: 3c durable cross-restart dispatch-identity (NOT-REACHABLE \u2014 no auto-replay, user-triggered only), 7-range multi-line comments (NET-NEW not regression, needs C9 client). Unblocks: C08 c8 REVISE_WIRED, C09 c8 live swap, session.transcript dispatch seam (C07+sidebar), C14 step-0 deferred-swap sweep"
  },
  "c01": {
    "status": "done",
    "passes": true
  },
  "c02": {
    "status": "done",
    "passes": true
  },
  "c03": {
    "status": "done",
    "passes": true
  },
  "c04": {
    "status": "done",
    "passes": true
  },
  "c05": {
    "status": "done",
    "passes": true,
    "note": "PR #525 landed, 8 review fixes confirmed; cluster 8 audited 2026-08-28: NOT a one-file swap \u2014 no board/lensBoard read command was ever registered; goes live via the wiring command-bundle change"
  },
  "c06": {
    "status": "done",
    "passes": true
  },
  "c07": {
    "status": "done",
    "passes": true,
    "note": "PR #529 landed; #540 landed the host-side session.transcript/rounds reads but the WRITE side (B10 cluster-6 SessionTurnLoop/buildAppTools) is unwired \u2014 server would return [], so the chat-data.ts client swap waits behind B10 c6. Dock identity + live-turn fold + compaction all work now over MemoryBridge"
  },
  "c08": {
    "status": "done",
    "passes": true,
    "note": "PR #530 landed; dual review upheld 8 findings (2 critical: exact-preview bytes, review-identity isolation), all fixed + confirmed. REVISE wiring LANDED #550 @b8f6d3b9 (flag DELETED, callers migrated; review seat FIX\u2192LAND: stale draftEdits shadow over rework fixed with CONDITIONAL overwrite \u2014 keeps pendingEditCount truthful; red-without-fix recorded). Posterity note: inline edit STARTED mid-rework can leave a stale shadow (narrow same-class race; fix = read fresh via useRennetStore.getState() in landRework if it bites). New ledger item recorded in c14 packet: client\u2192ask.* write sync (C18 era). requestConsent: Rai ruled DELETE \u2014 c19 packet"
  },
  "c09": {
    "status": "done",
    "passes": true,
    "note": "PR #536 landed (squash da8f815e); dual review opus APPROVE / Codex BLOCK, 6 findings upheld + fixed + confirmed by fresh opus. F1 reveal-gate, F4 report-identity validation, F5 lane honesty, F6 delta classification closed. F2 (per-round diff) + F3 (frozen-gen switcher) confirmed GENUINE B9 RoundRecord gaps (producer sets boardGeneration===mintedPatchsetGeneration at rounds.ts:319; no predecessor field) \u2014 parked as B9 follow-ups, code made honest (round-diff-pending + hidden switcher). Cluster 8 live swap B11-gated (single provider value in routes/app.tsx, seam bodies in rounds-data.ts)"
  },
  "c10": {
    "status": "done",
    "passes": true,
    "note": "PR #532 landed; dual-seat review (Codex restored) caught+fixed 9 findings incl. dropped pin/reset+appearance-reset port regressions. Honest-empty ledger: only agentsByHost is live-backed; model-council/glyph/worktree/tracker/guidance-write/remote-host detection await future backends. Pairing error-color nit deferred to A/B pass"
  },
  "c11": {
    "status": "done",
    "passes": true,
    "note": "LANDED #565 @f8c93b95 (closes the per-row menu inventory queued by #528). 95 commands audited: 1 exposed (github.disconnect) / 94 false, each row rationale'd in docs/developing/reference/command-menu-exposure.md, diffed against the real registry both directions. Rows now close-on-success/hold-on-failure (settle-driven, not close-on-dispatch). useGitHubAccount MIGRATED onto useCommand: it took bridge as a prop and called bridge.invoke('github.status') directly, violating the Track C standing law 'no component calls bridge.invoke' \u2014 so this APPLIED an existing law rather than adding scope. 33 bare test mounts wrapped in BridgeProvider (blast radius was 33, not the 8 the review estimated). UNPLANNED WIN FOR C14: strangler baseline drained \u2014 the github-account entry (6 no-direct-invoke suppressions) pruned from eslint-suppressions.json, verified 9->8 on main; one fewer legacy quarantine site. Review seat verified the 1/94 split by BUNDLING AND EVALUATING the real registry, not by trusting the doc. Failing-first test written against a0f29847 before the fix (DOM dump showed 'connected \u00b7 @rbutera' still rendered post-disconnect). Accepted deliberately: device-flow poll re-reads github.status each 2s tick (it IS the 'has this account changed yet' question, makes out-of-band sign-in land, short-lived, commented). COMMAND_CATALOGUE dead code filed as #564."
  },
  "c12": {
    "status": "done",
    "passes": true,
    "note": "PR #524 landed, 15 review fixes confirmed; cluster 7 audited 2026-08-28: NOT a seam swap \u2014 project.rename command was never registered; goes live via the wiring command-bundle change (with session.list/rename/pin/archive + 5 project-pref settings writers)"
  },
  "c13": {
    "status": "done",
    "passes": true,
    "note": "PR #535 landed (squash 47fb3c5d); dual review opus APPROVE / Codex BLOCK, 4 findings upheld + fixed + confirmed by fresh opus (detached-ref leak on FAB, fire-and-forget persist now observed+latest-wins, anchor proof on real chrome, malformed-MarkId negatives). Positive control verified on the useMergedRefs fix. Protocol registry (setCoachmarks) will conflict-fold when B11's ask.* lands second"
  },
  "c14": {
    "status": "pending",
    "passes": false,
    "note": "PRE-C14 GATE ADDITIONS (Rai, 2026-08-28): (1) DONE \u2014 titlebar fix LANDED #557 @f2ac71ff (real-device defect: traffic lights over wordmark; report->auto-update closed loop); (2) RELEASE BEFORE C14 \u2014 after last pre-C14 landing: gh workflow run auto-release.yml (SIGPIPE decide-step fix ca443db0 landed; this dispatch is its real test), verify green through publish + vX.Y.Z with macOS assets, Rai watches latios auto-update, THEN dispatch C14 \u2014 audit drives the released build where practical. | FLAKE LIST (Fournata, running): indexing-view 'Start a Review CTA scrolls itself into view' \u2014 non-deterministic, seen ONCE in a c11 positive-control run on 2026-08-28, passed both green runs either side of it, NOT caused by the c11 change. Deliberately not chased at the time; log it here so C14 triages it as a known flake rather than rediscovering it as a new failure. | FLAKE PATTERN, not just names (Fournata 2026-08-28): THREE flakes now \u2014 indexing-view 'Start a Review CTA scrolls itself into view', rennet-docs cold-graph 'rennet-ui omits declared dependency on core', and app-ui C13 coach anchors 'a disabled anchor never elects' (expected null, got 'Ready to Go'). All three share ONE property: they appear ONLY under full-gate concurrency and NEVER standalone. That is one environmental cause \u2014 parallel vitest workers plus nx task parallelism on a 16GB host under memory pressure \u2014 the same family as the documented cold-cache task-history artifacts, not three test bugs. PROVEN on the anchors one during the c11 landing: failed in full pnpm check, then passed standalone on main, standalone in the merged worktree, AND under the IDENTICAL full-suite command in the merged worktree (131/131 files, 966/966 tests). SUGGESTED C14 PACKET LINE: if a claim's verification fails, re-run the failing target SERIALLY (--parallel=1 / NX_PARALLEL=1, or vitest without threads) before recording a fail \u2014 otherwise the audit will manufacture phantom failures and bounce scoped fixes at workstreams that are not broken. | *** C14 IS BLOCKED ON #569 (not 'affected by') \u2014 DO NOT DISPATCH UNTIL RESOLVED (Fournata 2026-08-28) *** THE PLAYWRIGHT-ON-ELECTRON HARNESS CANNOT LAUNCH THE APP AT ALL: 'Electron: bad option: --remote-debugging-port=0', Electron 43.2.0 vs @playwright/test 1.62.0. PRE-EXISTING AND REPO-WIDE, NOT c20's doing \u2014 controlled: an untouched spec on main fails identically. WHY THIS RESHAPES C14: C14's entire method is 'every claim verified against the RUNNING client, driven, not code-read'. If no desktop E2E can launch, C14 CANNOT DRIVE THE APP BY MACHINE. Resolve one of two ways BEFORE dispatch: (a) fix the harness \u2014 likely a Playwright/Electron version-pin bump, worth timeboxing ~1h since a working harness changes the audit's whole shape; or (b) amend the C14 packet to state honestly which claims are MACHINE-verified and which are HUMAN-verified. Discovering this mid-audit is the expensive version. KNOWN UNPROVEN-BY-MACHINE ALREADY: c20's Electron E2E spec (8.1) written but NEVER EXECUTED, plus c20's traffic-light geometry, real window drag, and 5.3 scroll-under \u2014 proof path is Rai's manual verification on real hardware after the release he auto-updates to on latios. None of it may be recorded as machine-verified. | SHARPENED: the failure is at electron.launch, BEFORE any app code runs, so #569 blocks EVERY Electron spec identically including the four pre-existing ones. There is NO partial path where some claims are machine-driven and others are not \u2014 hence 'blocked on', not 'affected by'. The distinction changes what someone does about it. Timeboxed investigation (~1h) dispatched NOW rather than at C14's door: if it is a version bump, C14 keeps its real method cheaply; if it is genuinely hard, C14's packet gets amended KNOWING that instead of guessing. A working harness would also let the release be smoke-tested before Rai auto-updates to it on latios."
  },
  "c15": {
    "status": "done",
    "passes": true,
    "note": "LANDED #568 @78db856e (closes #541). THE LAST OF THE ORIGINAL 37. Codex round-1 BLOCKED with 11 findings; all taken at ROOT CAUSE across two sessions + two fix loops. Kills: #1 boards described the OLD diff (regeneration tail moved out of create-server into runBoardRegeneration; successor patchset recaptured BEFORE the review is read; landed decided by patchset-id inequality, not a flag). #2 fabricated lineage \u2014 carry-forward never fired in prod; real prior generation rebuilt from durable halves, absent-predecessor representable, two-round test proves it in production shape. #3 deletion-only rounds no longer read 'carrying forward'. #5 two contradictory terminal rows collapsed to one reported() wrapper. FINAL-REVIEW BLOCKER: a round whose report board never drafted STALLED THE RUN VIEW FOREVER (machine accepted only 'report' at 'committing'); route 1 is ordinary \u2014 'the agent ran and changed nothing'. Fixed at root: 'composed' terminal from ANY in-flight phase, mirroring 'failed'. Downstream lie also killed: a report-less round no longer promises boards held 'until its report arrives'. #9 skew stayed MINIMAL as Rai ruled (no negotiation, handshake, flag matrix, compat layer or dialog). #8's union split found a genuinely unrepresentable state and added 'drafted' rather than a fake verdict. TWO CONTROLS THAT PASSED WHILE THE BUG WAS LIVE WERE THEMSELVES FIXED (lane assertion now spans every emitted lane; round-trigger.test.ts:165 now passes an onProgress sink instead of asserting only 'no crash'). Merge resolved registry count to 96 against the ACTUAL merged ABSORBED_IDS array (base 93, c15 +1, main +2); c11's day-old drift guard immediately caught c15's session.roundEvents, now in the exposure inventory (needs a reviewId, cannot accept {}, stays commandMenu:false). GATE_EXIT=0 on the merged tree. Filed not fixed: #566 dedup, #567 duplicate terminal row (cosmetic \u2014 the machine ignores the second row). FOR C14, NOT A REGRESSION: eventsError is now unread, so a failing progress read alone surfaces nowhere \u2014 the ledger was the wrong consumer and no surface ever consumed it correctly."
  },
  "c16": {
    "status": "landed",
    "passes": true,
    "note": "LANDED #553 @9a850be7 (closes #542). Per-scenario overrides per Rai's ruling \u2014 sibling-leak structurally impossible, flip-to-red proven. Review FIX\u2192CONFIRMED: app-ui's drifted local council-table copy DELETED (6 cells lied; server honest-present at source), REVIEW_ROLE_DEFAULTS now test fixture, Reset doc per-role truth, adopted cleared on refetch-settle. Surface truth: two columns (Dual + provider-resolving Single) over three scenarios. First of the snapshot set down \u2014 c17/c18 rebase over it at landing."
  },
  "c17": {
    "status": "landed",
    "passes": true,
    "note": "LANDED #560 @a6e1b520 (closes #543, #533, #534). Full host/tool detection: forge.detect + per-host daemon.status + served harness.hosts/setEnabled + forge-enable read (amendment A) + forge.hosts (amendment B) + Reconnect + Update Daemon, real-daemon E2E with real-gh rename control. Codex 8-finding FIX -> opus CONFIRMED (non-strict-arms doctrine endorsed). Fold judgments: c16 session-only disabled set SUPERSEDED by served store; three c16 tests converted harness.detect->harness.hosts (real integration catch); three not-live-yet doc claims dropped (all went live today). FOR C14: detection-eviction remove/restore cycle has MANUAL-ONLY E2E coverage (helper unit + structural eviction committed) \u2014 scoped bounce if the sweep wants it committed. RENNET_DISABLE_HARNESS now also suppresses codex locally. Snapshot 87."
  },
  "c18": {
    "status": "landed",
    "passes": true,
    "note": "C18-COMPLETE: slices 1-2 #562 (board.read, session mutations, project.rename) + group-A #563 @3de83f82 (closes #551). Five prefs served for real: registry keys, per-repoKey rung on the project config.json, tracker kind-floor law shared by chip+retrieval, id-addressed guidance writer, DERIVED projectEditsPersist (skew-honest). Registry 95. Both Codex fix loops closed CONFIRMED."
  },
  "c19": {
    "status": "landed",
    "passes": true,
    "note": "LANDED #559 @0053c25e (closes #552). Token deleted end to end (snapshot 80->79, both clients); verdict folded into publishCompositionId/assertCompositionFresh \u2014 flip-without-recompose refused, red-proven from committed baselines. Desktop verdict override made durable (was local-only; packet falsehood caught by implementer). Review FIX->CONFIRMED. Follow-ups #555 #556. Survivors untouched."
  },
  "c20": {
    "status": "review",
    "passes": false,
    "note": "NEW (Rai 2026-08-28, approved from live spike demo): corner-slot macOS chrome \u2014 leftmost pane owns the lights, wordmark + rail deleted, chat toggle on main view. Lands BEFORE C14; closes #558. Packet: openspec/changes/c20-corner-slot-chrome/ | PROPOSER IN FLIGHT (Fournata 2026-08-28): authoring proposal.md + tasks.md from the packet, porting the design (not the code) off spike/corner-slot-demo. Must ship in the pre-C14 auto-release Rai watches land on his own machine. | MANDATORY FOR C14 (coordinator ruling 2026-08-28): the implementer MUST do the packet's \u00a71/\u00a75 INVENTORY RE-RULING ANNOTATIONS \u2014 inventory lines assuming the wordmark's OLD placement, the 48px rail, and a chat-header collapse control get ANNOTATED IN PLACE with the new ruling, never silently deleted and never left stale. C14 audits against the inventory: a line still asserting a 48px rail after c20 deletes the rail is a lie in the record, and a vanished line is worse (the audit cannot distinguish deliberate re-ruling from oversight). Own task cluster, own verification that fails on an un-annotated invalidated line. Sent to the proposer so it lands in tasks.md rather than depending on orchestrator memory. | PROPOSAL APPROVED 2026-08-28: 8 clusters / 30 subtasks, footprint app-ui + apps/desktop ONLY (no server/protocol/core/adapters). Five packet-vs-repo reconciliations found, two unanticipated. RULINGS: (1) DESIGN.md:79 translucency conflict -> AMEND as a NARROW JUSTIFIED EXCEPTION (floating chrome in full-bleed may use a translucent blurred ground; opaque grounds remain the rule everywhere else, Rai 2026-08-28 approved live watching the chips render). Original prohibition NOT deleted or generalized. Provenance: :79 is a docs-rebuild commit, not a Rai decision commit \u2014 agent-authored rule clarified against a Rai-authored ruling. (2) always-mounted chat dock double-mount = the packet's named regression class, caught pre-implementation; dockOpen gating kept, double-mount DOM test is the cluster's positive control. (3) ghost surface: re-grep, record the verdict, don't hunt. (4) floating mount in routes/layout.tsx not TopBar \u2014 a collapsed sidebar on Settings would otherwise have no corner slot AND no drag region. (5) inventory line 694 [ws:C11] included beyond packet C3/C7 scope (no silent drops); two-way control on check-inventory-tags.mjs is the guard C14 needs. RISK: 5.3 scroll-under has no crisp DOM assertion \u2014 implementer reports the limitation rather than asserting something vacuous; 8.1 Electron E2E is precedent-free \u2014 may reduce to geometry + manual-proof note. SEQUENCING: c20 overlaps c11's app-ui shell area, so SERIALIZED \u2014 land c11 first, then dispatch c20's implementer over post-c11 main. Still lands well before C14. | IMPLEMENTER DISPATCHED 2026-08-28 in /Volumes/ExternalNVMe/home/dev/rennet-wt-c20, branch feat/c20-corner-slot-chrome cut from post-c11 main @21dc3c44. Serialization satisfied: c11 landed first. | ALL 8 CLUSTERS COMPLETE @389385c3 (GATE_EXIT=0, 29/29 subtasks, three positive controls each red-then-green, including the two-way inventory checker proving BOTH a stripped annotation and a deleted line fail). STANDOUT: cluster 2's ordering fix \u2014 deleting the rail first would have left an intermediate commit with NO way to expand the sidebar, so the floating slot mounts in cluster 2 and narrows in cluster 3. Same final code, no capability gap at ANY commit: Rule Zero applied to COMMIT SEQUENCING, not just the end state. Also caught a real theme-contract violation during the gate (a consumer stylesheet defining --rn-*). 8.3 done on BOTH prohibitions plus R26 in contracts-and-rulings.md (a rulings-table reader would otherwise have been left wrong). 8.2 correctly did NOT override trafficLightPosition \u2014 8.1 could not measure it, and tuning blind is guessing dressed as a fix. 8.1 UNCHECKED/ANNOTATED as 'spec written, never executed, blocked by #569' \u2014 the implementer ASKED rather than quietly checking the box, which is the right instinct; a checked box claims a verification that did not happen. Dual review wave dispatched (Codex round-1 + fresh opus seat). | 8.1 CORRECTION IN @6fcfac80: box UNCHECKED and annotated (28 checked / 1 open), the three unproven claims enumerated with Rai's manual verification named as the proof path, spec header carries #569. Implementer's own words, exactly right: 'until Rai looks at it on a real window, all three are asserted by construction and by reading the CSS, not demonstrated.' No full re-gate required \u2014 tasks.md is not a gate input and the spec edit is a comment; GATE_EXIT=0 stands at 389385c3. Dual review seats' reads stay valid: the correction changed no gated behaviour."
  },
  "c21-session-mint": {
    "status": "pending",
    "passes": false,
    "note": "NEW, PRE-C14, from RAI'S REAL-DEVICE TESTING of v0.3.37 on latios (2026-08-28). LEDGER HOLE \u2014 SESSION MINTING WAS NEVER BUILT. New Chat's picker selects a target and clicking a row DOES NOTHING. new-chat-view.tsx:48-50 says live minting from a row is 'the GATED cluster 7 (B9) behind new-chat-mint.ts' \u2014 B9 LANDED, the gate cleared, and nobody built it. new-chat-mint.ts does not exist, and there is NO session mint/create command in the registry (only list/rename/setPinned/archive/rounds/transcript). So the headline C12 claim 'row click mints a session AND claims the target' has no server path at all. Same species as the board-read gap c18 filled, and it is NOT on the wiring ledger. SCOPE: register the mint command + handler, build new-chat-mint.ts, wire the row click. WHY PRE-C14: C14 would fail every \u00a710 claim about starting a review, and 'start a review' is the product's front door. Footprint: protocol + server + app-ui/project. | SEQUENCING: #569 goes FIRST if a choice is forced \u2014 a working harness is the only thing that smoke-tests the release before it reaches Rai's machine. c21 remains a hard PRE-RELEASE blocker regardless: shipping a release Rai auto-updates to while the product's front door does not open would be worse than shipping late."
  },
  "c22-knowledge-pass": {
    "status": "pending",
    "passes": false,
    "note": "NEW, PRE-C14, from RAI'S REAL-DEVICE TESTING of v0.3.37 on latios (2026-08-28). POSSIBLE LIVE BUG \u2014 CONTEXT MAP GENERATES NO KNOWLEDGE. Unlike the minting hole this IS wired: create-server.ts:1579 runKnowledgePass calls knowledgeSwarmRuntime.runForRepo inside the rehydration lifecycle, so it should run \u2014 and produces no knowledge on Rai's machine. HONESTY HAZARD FOUND WHILE VERIFYING: runKnowledgePass returns `outcome.status === 'ok' || outcome.status === 'skipped'` \u2014 a BARE BOOLEAN, so a SKIP and a SUCCESS are indistinguishable to every caller, while the swarm itself picks skip-vs-incremental-vs-full from the stored prior set's identity. A wrong identity read would make it skip forever WHILE REPORTING SUCCESS. INVESTIGATION FIRST (read-only): run project.process against a real repo on this host, capture the swarm's actual outcome status and any per-partition failures, determine whether it is skipping, erroring silently, or failing at the model boundary (#544/#546 fixed two harness-compat bugs \u2014 check whether the swarm's council calls route through the fixed path). Report before fixing. If the boolean is masking failures, the fix MUST surface the real outcome, not just repair the bug behind it. Footprint: core/knowledge + server/runtime."
  },
  "e569-e2e-harness": {
    "status": "implementing",
    "passes": false,
    "note": "ISSUE #569. RAI RULED DIRECTLY (2026-08-28): upgrade Playwright AND Electron to the latest MUTUALLY-COMPATIBLE versions \u2014 NOT a minimal pin-back, NOT a workaround, and NOT amending C14's packet around it. Fix the harness properly. Current break: 'Electron: bad option: --remote-debugging-port=0', Electron 43.2.0 vs @playwright/test 1.62.0, failing at electron.launch BEFORE any app code runs \u2014 so it blocks EVERY Electron spec identically, including the four pre-existing ones. Pre-existing and repo-wide (controlled: an untouched spec on main fails identically), NOT caused by c20. LATEST COMPATIBLE, NOT LATEST INDEPENDENTLY \u2014 picking each side's newest in isolation is exactly how 43.2.0 met 1.62.0. If they cannot both be latest, take the newest compatible PAIR and report the constraint; never silently pick one side. Dependency standard + npmrc min-release-age=7 planned around, NOT overridden. ACCEPTANCE BAR: the four pre-existing desktop E2E specs actually RUN AND PASS (not merely that electron.launch stops erroring), plus rennet-desktop:package-smoke, plus full pnpm check with a GATE_EXIT line. c20's corner-slot spec runs as an informational fifth \u2014 its verdict is c20's business. Electron majors move Chromium, so app-level breakage (renderer APIs, daemon supervisor, apps/desktop/src/main) is IN SCOPE to fix: an upgrade that green-lights E2E while breaking the app is worse than the status quo. *** SEQUENCING PRIORITY: this is worth MORE THAN ITS OWN SCOPE. A working harness is the ONLY thing that would smoke-test the release before it auto-updates onto Rai's machine \u2014 right now NOTHING does. So it lands ahead of c21 session-mint if a choice is forced: a broken release reaching him is worse than a late one. Footprint (package.json + lockfile + apps/desktop) collides with c20, so it folds c20's main and lands AFTER c20 \u2014 implementation runs in parallel, landing serializes. Must land BEFORE the release and obviously before C14. *** Converted in-flight from a timeboxed spike; timebox lifted. ONE agent on these files."
  }
}
