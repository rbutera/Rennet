# B11 — exits-backend · tasks

Serial clusters; one cluster per session; each ends on a green gate + commit. Cluster 6 is the packet-verification cluster (E2E + positive controls able to fail). No stubs — an incompletable task stays unchecked with a note. The `publish.requestConsent` consent token is PARKED (do not touch). Do not edit `app-ui` (client swap is C9) or `protocol/src/session/model.ts` (frozen).

## Cluster 1 — durable asks: event log + fold + store (clause: asks durable host-side; one write path)

- [x] 1.1 `packages/protocol/src/` (extend, not `session/model.ts`): the ask **event** union and the projected **ask state** shape — `AskEvent` = stage / edit / retire / restore / quote-open / quote-reply / quote-close / verdict-override-set / verdict-override-clear / line-comment-set / line-comment-clear, each carrying a session id, a monotonic seq, and the fields the client model already names (`StagedAsk`/`RetiredEntry`/`QuoteThread`/`verdictOverride` in `app-ui/src/store/review.ts` are the authoritative shapes to mirror; `focusedThreadId`/`draftEdits` stay client-transient — not durable). Register the `ask.*` command names + their `CommandInput`/`CommandOutput` (each write returns a **receipt** that reverses it). Zod-first; `parseCommandInput`/`Output` cover them.
- [x] 1.2 `packages/core/src/exits/ask-projection.ts` (new): the pure **fold** — `foldAsks(events): AskProjection` collapsing the event log to current `{ stagedAsks, lineComments, quoteThreads, retired, verdictOverride }`, and `receiptFor(event): AskEvent` producing the inverse event (receipt-is-undo: stage↔retire-as-undo, edit↔prior-body, quote-reply↔drop-last, override-set↔prior-value). Pure, no I/O. `core/src/exits/index.ts` is the folder surface (the packet's `core/exits/`).
- [x] 1.3 `packages/adapters/src/ask-log-store.ts` (new): a file-backed append-only per-session `AskLog` (`append`/`read`/`readProjection`), following B10's persistence-engine precedent (same serialization + schema-parse on read as `session-store.ts`/`file-thread-store.ts`). Append is the ONLY mutation; the projection is `foldAsks(read())`, never a second stored copy.
- [x] 1.4 Tests: `ask-projection.test.ts` — every event folds; a receipt applied after its event returns the projection to the prior state (receipt-is-undo, per event kind); seq is monotonic. `ask-log-store.test.ts` — round-trips a log through disk, schema validates on read, append is additive (no rewrite).
- [x] 1.5 Cluster gate green. Commit.

## Cluster 2 — ask command surface + create-server wiring; reload survival (clause: one write path; survive reload)

- [ ] 2.1 `packages/server/src/dispatch/ask.ts` (new): the `ask.*` handlers — each parses input, **appends one event** to the session's `AskLog` via the store, and returns the receipt (the sole write path; no handler mutates a projection directly). A retire/restore/override handler is symmetric with its receipt. Register in `dispatch/index.ts` beside `publish`/`review`.
- [ ] 2.2 Wire into `create-server.ts`: construct the `AskLogStore`, expose `ask.read` (the projection) as the session-open read, and **emit projection updates to clients** on every append (private contract + R19 `projection.ts` — the ask projection is model/anchor data; route host paths through `toRepoReference`, prose through the blanket scrub, add no new leak). A reconnecting client reads the current projection; nothing is client-derived.
- [ ] 2.3 Tests: `ask-dispatch.test.ts` — each command appends exactly one event and returns a reversing receipt; applying the receipt (a second command) restores the projection; the projection read after a simulated restart (new store over the same log file) equals the pre-restart projection (the reload-survival unit, the E2E's core in miniature).
- [ ] 2.4 Cluster gate green. Commit.

## Cluster 3 — publish.compose sources the durable ask projection (clause: PR body draft; two-strata review; publish submission)

- [ ] 3.1 Extend `dispatch/publish.ts` `publish.compose`: source the outbound composition from the durable ask projection (via the store) instead of only `review.dispositions` — `mode:"review"` composes the two-strata review (`reviewCommentsFromDispositions` → the ask-projection equivalent: body asks + path-grouped line comments), `mode:"pr"` feeds the ask set + verdict override into `draftPrBody`. Verdict override, when set, wins in `deriveReviewEvent`. `compositionId`/exact-preview/`raisePublishReady` unchanged; the consent path untouched (PARKED). This is what lets C9 swap `handoff-data.ts` to read the composed living draft.
- [ ] 3.2 Reconcile disposition-vs-ask sourcing: confirm whether the durable asks supersede or augment `review.dispositions` as the compose source (the client seam treats the durable projection as the living-draft authority). Source from the projection; keep the output shape (`CommandOutput<"publish.compose">`) byte-identical so `app-ui/src/handoff/handoff-data.ts` `composeReviewDraft` still parses — no protocol churn.
- [ ] 3.3 Tests: `publish-compose-asks.test.ts` — a review with staged asks + line comments + a verdict override composes the same two strata and derived verdict from the projection; an empty ask set composes nothing to post (honest `unavailable`/empty, matching today); the composed bytes still satisfy the `publish.review` exact-preview re-derivation (the compose↔review round-trip holds).
- [ ] 3.4 Cluster gate green. Commit.

## Cluster 4 — round work-order dispatch: asks → one dispatch, serialized (clause: round work-order composition)

- [ ] 4.1 Wire `createRoundsRuntime` + `SessionEntry` into `create-server.ts` (the trigger B9 deferred, tasks 5.1/6.2): compose the runtime from its open seams following the `createKnowledgeSwarmRuntime`/`createProjectScoutRuntime` precedent (`onBoardArrival` → board broadcast, `persistBoardMeta` → the board-meta store, `composeTurn`/`readPrompt`/harness ports resolved as B9's 6.2 note specifies). No edit to the pipeline's pure logic.
- [ ] 4.2 `packages/server/src/dispatch/round.ts` (new): the round-dispatch handler — takes the dispatched asks, composes them into **one** work-order via `core/handoff-compose` (`composeHandoffBundle` over the projection's asks), and hands it to the rounds runtime **serialized per session** (one round in flight; reuse the runtime's per-session serialization, do not add a second lock). **Idempotent**: a re-dispatch of the same asks while a round is in flight (or already dispatched) returns the in-flight round, not a second dispatch.
- [ ] 4.3 Tests: `round-dispatch.test.ts` — dispatched asks compose exactly one work-order; a second dispatch of the same asks yields exactly one dispatch (the idempotency positive control — assert the injected runtime saw one call); a round dispatched while one is in flight is refused/coalesced, not raced.
- [ ] 4.4 Cluster gate green. Commit.

## Cluster 5 — living-draft rework (one-shot workers) + PR lane ripening (clause: rework + quote-match carry; idempotent push; ripening)

- [ ] 5.1 `packages/server/src/dispatch/rework.ts` (new, or extend `dispatch/review.ts`): the real span-rework command backing the client's gated `reviseDraftSpan` seam — dispatch a **one-shot worker** through B9's `ReworkQueue` (`session/rework-queue.ts`), serialized per document, never resuming the interactive cursor. **Quote-match carry**: the reworked span re-anchors across the regenerated draft by matching its quoted text via `core/lineage-matcher` (reuse; do not add a second matcher). The write routes through the sanctioned staging path (B9 reconciliation 4), never a second writer.
- [ ] 5.2 PR lane **ripening across rounds**: the own-branch PR draft re-composes as each round lands — after a round's worker return, re-run `publish.compose(mode:"pr")`'s composition and re-raise publish-ready (idempotent by derived id, the existing `raisePublishReady` path). No change to `publish.submitPr`'s idempotent push+open-PR — verify it, don't rebuild it.
- [ ] 5.3 Tests: `rework-dispatch.test.ts` — a span-rework enqueues one one-shot worker (not a resident-cursor turn); two reworks on one document serialize; a reworked span re-anchors by quote match after regeneration (inject a regenerated draft; assert the carried anchor). `pr-ripening.test.ts` — a landed round re-composes the PR draft and re-raises publish-ready idempotently; `submitPr` opens/reuses one PR for the same head (the idempotency positive control).
- [ ] 5.4 Cluster gate green. Commit.

## Cluster 6 — docs + packet verification (positive controls able to fail)

- [ ] 6.1 Update `docs/developing/concepts/handoff-and-exits.md` to describe the durable-asks backend as shipped: the one write path (command → event log → projection), receipt-is-undo, the three exits and where each composes, round-dispatch serialization/idempotency, rework one-shot workers + quote-match carry, PR ripening. Draft/preview/post language; push ≠ publish; nothing posts without Rai clicking post. Add a mermaid fence for the write path if it reads clearer. Docs style guide + good-docs standard.
- [ ] 6.2 E2E (a) — durable asks survive reload: stage asks (body + line comment + quote thread + verdict override) → kill the host → restart → the projection is intact. Positive control: a variant that drops one event on restart must fail the assertion.
- [ ] 6.3 E2E (b) — dispatch a round work-order **twice** → exactly one dispatch. Positive control: a variant that omits the idempotency coalescing must observe two dispatches (fails).
- [ ] 6.4 E2E (c) — compose + preview a GitHub review draft for a real PR **without posting** (`dryRun` path; assert nothing left the machine and the previewed bytes equal the compose bytes). Positive control: a mutated payload must fail the exact-preview re-derivation.
- [ ] 6.5 Full gate: `pnpm check` green. Output the completion sigil and flip `BUILD-STATUS.json` B11 → passes.
