# B07 — related-context (#461: dossier retrieval + project scout)

Packet: `context.md` (scope authority). Plan row B7. Blocked-by B3 (dossier shape, landed) + B6 (L0 hand-off seam, landed). Authored 2026-08-27 against main @ 229fadc6 (B06 landed).

## What this change does

Implements #461's resolution: related context is agent food first — a bounded, deterministically serialized dossier that lens drafters, the orchestrator, round workers, and the round-report drafter receive verbatim.

- **`adapters/related-context.ts`** — deterministic ref extraction first (branch name, commit messages, PR body/title → issue refs, zero cost), then `gh` fetch (`gh api`, `gh pr view --json`; token stays inside `gh` — no octokit on this path, #483 reversal stands), then the `related-context-retrieval` light-tier council worker enriches: fetches every referenced ticket, follows links one hop, emits the bounded **related-context dossier** (protocol `dossierItemSchema` items: id / tracker / title / state / bounded body / acceptance criteria / URL / provenance / fetched-at). JIRA/Linear config-only: base URL + token env var NAME from settings; the worker hits their REST directly (env read at run time; the token itself is never stored).
- **Delta-resident (L1), inlined verbatim**: the dossier is the `dossier` parameter B05's `buildDeltaPacket` already takes. Standing tracker knowledge flows through B6's swarm (L0) — no separate mechanism here. Raw payloads (full comment threads, linked tickets) persist durably beside the dossier for depth on demand; they never enter the dossier.
- **`adapters/project-scout.ts`** — the #461 §4 detection set at project add (re-runnable): zero-cost deterministic pass (git remotes → GitHub detection, JIRA/Linear markers, README/logo files, package manifests) first; the `project-scout` council seat fills only what determinism could not; every answer carries provenance (detected vs guessed). Cosmetic results (logo) go to settings/UI only, never agent context. Seed guidance rules land in the existing repo-layer guidance catalogue.
- **Config on the #476 ladder**: tracker keys join `SETTINGS_REGISTRY` (scout → detected layer, user answers → global rung, optional repo file). Missing config = a typed in-chat ask that persists once answered — never a modal, never a gate (Rule Zero).

## Out of scope (consume shapes only)

Add-project UI (C12); standing tracker knowledge through the swarm (B6 owns the pipe); drafters consuming the dossier (B8); full round scheduling (B8). `DossierItem`/`serializeDossier`/`DOSSIER_BODY_MAX_CHARS` and the council job ids are frozen contracts — consumed, never re-modeled.

## Reconciliation ledger

1. **Council ids exist in protocol; core rows are missing.** `project-scout` and `related-context-retrieval` are already in `COUNCIL_JOB_IDS` (`manifests/index.ts:57-58`, B03). B06 added only its own two catalogue rows. B07 adds the `JOB_CATALOGUE` entries + three assignment-table rows. Packet says "project-scout (medium)": per B06 reconciliation 2, *medium is the model class, not a tier* — `project-scout` lands HEAVY-tier with medium-class picks (sonnet-5/medium in both/claude-only, terra/medium codex-only `[extrapolated]`); `related-context-retrieval` lands LIGHT-tier (luna/low both+codex-only, haiku/low claude-only `[extrapolated]`).
2. **`gh` first-class beside the octokit transport — no conflict.** The existing `github-*` adapters run on injected Octokit (the v4.2 migration; auth/publish/changeset paths). #461 point 1 rules retrieval runs on `gh` (`gh api`, `gh pr view --json`). Verdict: `related-context.ts` takes an injected gh RUNNER (execFile seam, `git-range-diff.ts`'s `GitExec` pattern) so tests never spawn a process; the octokit paths are untouched. Credential story stays honest: `gh` holds its own token; Rennet never reads or stores it on this path.
3. **Settings rows land now.** #476 decision 4 deferred #461's registry rows "until the engine consumes them" — B07 IS the engine consuming them. New `SETTINGS_REGISTRY` declarations (issue-tracker section): tracker kind, project key/prefix, JIRA/Linear base URL, token env var NAME, plus the §4 non-tracker facts (worktree base-dir convention, gate command; default branch only if not already a resolved fact — implementer verifies). Scout writes the DETECTED layer: the resolver takes detected values at resolve time, so scout facts need a durable detected-layer store — the implementer inspects how detection persists today (locus precedent) and records the actual mechanism in this ledger.
4. **deterministic E2E — RULED (track-b, 2026-08-27): approved as proposed.** The packet E2E ("against a real Rennet PR") cannot hit live GitHub in the gate. Chosen approach (B05 amendment-8 + B06 reconciliation-5 precedents): a frozen fixture captured once from a real Rennet PR (its `gh` JSON payloads recorded into the repo, own-repo material only) drives the injected gh runner; the light-tier enrichment runs a deterministic stub `runTurn`; council routing for both new ids is proven by contract tests (right harness port, right model/effort per scenario). No live network, no live model, in any gate path.
5. **Dossier durability: `.rennet/` store now, generation attach at B8.** #461 point 5 says "stored durably on the patchset generation, re-run per round under append-then-freeze" — generation lifecycle is B8/B9's. Verdict: dossier + raw payloads persist under the knowledge-store precedent (`~/.rennet/projects/<esc>/dossier/…`, keyed by review target + patchset ref), `serializeDossier` as the canonical bytes; the generation-attach and per-round re-run scheduling are B8 seams, documented at the store's JSDoc. The store is the durable home either way; B8 re-keys nothing.
6. **Raw payloads behind a context tool — storage now, binding where the tools live.** The existing context-tool surface (context manifest / `context.knowledge`) is the delivery point; B07 stores raw payloads durably and exposes a read seam; if binding requires a new command-registry row, it is NOT added here (B10 owns dispatch binding, B03 precedent for `patchset.readSpan`). Implementer records what the existing surface allowed.
7. **The in-chat ask is a typed fact, not a conversation.** The orchestrator that would ask lives in B8. B07 ships the detection (`missingConfig` facts on the retrieval/scout results, provenance-tagged, e.g. "JIRA key seen, no base URL") and the persistence path (the answer is an ordinary settings write on the ladder). No modal, no gate, nothing blocks: retrieval proceeds without the unconfigured tracker and says so in the result.
8. **Session wiring: minimal choke point now, rounds at B8.** "Runs per review session, re-runs per round": B07 wires the retrieval seam at the existing review-session start choke point (where `live-review-backend`/server start review state today — implementer inspects and records the actual point, B04 precedent); per-round re-runs ride B8's round runner. The seam takes the same injected deps as the swarm runner (B6 pattern).

## Cluster amendments

9. **Amendment (cluster 4): the detected-layer mechanism (reconciliation 3's inspection).** Locus's `detected` offer is recomputed free at every resolve; scout answers are not (a council seat ran). So scout facts persist at `~/.rennet/projects/<esc>/scout.json` (knowledge-store home pattern, atomic write) and `scoutSettingsOffers` reads them back as the resolver's `detected`-layer offers — only facts with provenance `detected` enter the ladder; a `guessed` fact renders in the questionnaire but waits for the user's confirming settings write. Default branch got NO settings row: it is already a resolved fact (project-discovery's origin/HEAD detection) and a row would be a second source of truth. `RefSource` gained one member (`scout-detection`) so scout missing-config facts reuse the shared `MissingConfigFact` shape instead of forking it.

10. **Amendment (cluster 5): the recorded session wiring point (reconciliation 8).** The choke point is `createLiveCanvasOpsBackend` (`adapters/live-review-backend.ts`) — every review open routes through this composition root (the server's `createDesktopReviewBackend` wraps it), and it already hosts the B6 initial-knowledge fire-and-forget this wiring mirrors: retrieval runs in the background (a review never blocks on it), errors report through `onKnowledgeError`, and the result persists to the `DossierStore` keyed `target@patchsetId` (`pr-<n>` from `postTarget`, else `headRef`, else `local`) — a stored record gates a refire on the same patchset, and a re-capture (new patchset id) re-runs naturally. The enrichment seat resolves through `councilSeatTurn` on the same lazily-resolved Claude port as the knowledge seed (honest claude-only availability, B6 precedent); absent a port the deterministic dossier still lands. Two consequences recorded: `RetrieveRelatedContextDeps.repo` became OPTIONAL (a local capture has no forge repo — a bare `#123` is then a typed failure while qualified/URL refs still resolve), and `LiveBackendDeps` gained optional `trackerConfig` (JIRA/Linear endpoints are settings-ladder values; B8's orchestrator, which owns per-session settings reads, supplies them — absent, seen tracker keys surface as the reconciliation-7 missing-config facts).

## Review-fix amendments (PR #519 round 1)

11. **Tracker routing rides configured prefixes, not endpoint existence (Codex 4).** `TrackerEndpointConfig` gains `projectPrefixes`; a key routes to the endpoint whose prefixes claim it. The old behavior — every seen prefix labeled JIRA the moment a JIRA endpoint existed, Linear-only configs routing nothing — is gone, and `prefixesSeen` with it. Unclaimed prefixes stay `unknown` → missing-config facts.

12. **High-signal single keys are believed (Codex 7, upheld half).** A single unconfigured `PROJ-42` in the branch name or PR title now yields the tracker-key ref (→ the missing-config fact #461 calls out); the twice-seen plausibility rule survives only for low-signal prose (commit messages, PR body). The finding's dedup half was REFUTED by seat 1: `#42` vs `owner/repo#42` canonicalization happens at the fetch layer (`fetchGithub`'s repo-resolved visited set), not in extraction — no change there.

13. **One-hop link following covers every fetched payload (Codex 8, downgraded P2).** JIRA and Linear fetches now surface their body text as hop-extraction input alongside GitHub bodies, and configured tracker keys found one hop out are fetched through the same visited-set logic (one hop total; unconfigured hop keys stay out — hop prose is low-signal).

14. **The dossier has an aggregate bound (Codex 5).** `DOSSIER_TOTAL_MAX_CHARS` (64 KiB, adapter-owned — the protocol item shape is untouched) caps the canonical serialized dossier: items over the cap drop WHOLE, last-fetched first, each recorded as an `omitted` fact on the result. Model-produced `acceptanceCriteria` is bounded at application like every body. The e2e's limit no longer scales with item count.


15. **Scout: tracker-specific URL evidence outranks the key shape (Codex 9).** `ABC-123` is the shape BOTH trackers use, so a `linear.app` link now decides `trackerKind: linear` even when keys repeat, and the dominant prefix classifies as the LINEAR project key. JIRA classification requires the key evidence without a Linear marker.

16. **Harness discovery cannot skip the deterministic scout (Codex 10).** Each port resolver's failure is isolated (`.catch(() => null)`): a rejected discovery narrows availability, and the zero-cost deterministic pass always runs and persists.

17. **The dossier pair publishes atomically, and persisted state is parsed, never cast (Codex 11 + 12).** The store now writes ONE envelope (`record.json`: canonical `serializeDossier` bytes + raw payloads) through the existing atomic write — a crash cannot leave a dossier whose raw depth is missing, and the refire gate cannot see a torn pair. Both the record and `scout.json` are zod-parsed at load: a malformed or hand-forged file (e.g. an out-of-vocabulary provenance) reads as an honest absence instead of walking typed into the settings ladder. Format changed freely — no durable records pre-B7.

## Verification (packet)

`pnpm check` green. E2E (ruled approach, reconciliation 4): against the frozen real-Rennet-PR fixture, extraction → retrieval produces a dossier where every item carries provenance + fetched-at, total serialized size respects the bound, and the dossier inlines into a B05 `buildDeltaPacket` call without truncation. Positive controls that can fail: oversize body → bounded (schema rejects / item truncated per the shape's rule); drop provenance from a fixture item → schema rejects; break the ref extractor → zero items and the e2e's item-count assert fails.
