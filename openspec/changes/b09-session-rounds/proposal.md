# B09 — session-rounds (#466 + #486: the durable session root and the rounds loop)

Packet: `context.md` (scope authority). Plan row B9 — the consuming turn of the critical path B8→B9→B11. Blocked-by B3 (frozen `protocol/src/session/model.ts`, landed); interlocks with B8 (lens pipeline, landed and deliberately unwired — B09 is its consuming turn). Authored 2026-08-27 against main @ ff93d683 (B08 landed, PRs #521+#526).

## What this change does

Builds the **runtime** for the durable session and the rounds loop over the shapes B03 froze in `packages/protocol/src/session/model.ts` (#466, header: *"Shapes only: the state machine, locks, and rework queue are B9's"*). No protocol edit; every shape is consumed, never re-modeled. This is where B08's `runLensPipeline` finally gets a caller.

- **Session = first-class durable root** (#466 res. 1–2). A new session runtime owns the harness cursor, the threads, and the claim (`SessionModel`). A review attaches `1:0..1` by `reviewId` (referenced, not absorbed); a no-target session has no claim and **upgrades in place** when a target binds; the claim **locks once boards exist** (a generation has been minted) — a new target then requires a **new session**. Archive is the only release (v1 soft delete via `archivedAt`). A file-backed `SessionStore` (adapters, following the `file-thread-store`/`file-project-store` precedent) persists it.
- **Harness = cursor-resume** (#466 res. 3, the T3 pattern). Interactive turns run **fresh-process-per-turn + SDK `resume`**: the CLI owns the transcript, compaction, and prompt cache; Rennet persists only the `HarnessCursor` pointer into it and re-passes it each turn. This adds the resume path the port left "absent by design" (reconciliation 2). Rennet owns the turn loop: **serialize turns per harness id** (one in flight at a time), **re-pass options every turn**, and a **resume-vanished fallback** — when the harness transcript is gone, rebuild context honestly ("context rebuilt") while the **boards stay canonical**.
- **Compaction surfaced honestly** (#466 res. 3 ripple). The harness's own compaction is shown, never hidden and never estimated: a **`compact_boundary` row** in the turn stream, a **PostCompact summary** surfaced to the reader, and an **ask-don't-estimate meter** (report what the harness says about its context, do not fabricate a token budget). Honest copy, not a gate.
- **Anchored threads** (#466 res. 7). Thread **content lives only in the session transcript**; boards and the diff hold **anchor→thread references** (`SessionThread` + `ThreadAnchor`). One mechanism carries code-line comments, prose-quote comments, and Explain; an `Ask` rides on the anchored arm when one is minted.
- **Staging writes, one path** (#466 res.). Session and round mutations that touch boards go **tool call → event log → projection** through the existing B04 choke point (`boards-runtime` → `projection` → `whiteboard-client`) — B09 routes through it, never a second writer (reconciliation 4).
- **Row click mints session AND claims target; re-entry reattaches; pipeline idempotent per session.** A New-chat row resolving to a branch/PR mints a session and claims the target in one act (`Claim` = branch + PR as one thing); re-entering mid-generation **reattaches** to the live session rather than starting a second; the drafting pipeline **starts idempotently per session** (a second entry never double-starts it).
- **Rework = one-shot workers outside the chat, serialized per document** (#466 res.). Rework runs as one-shot workers (not the interactive session), **serialized per document** so two edits to one board never race.
- **Rounds loop state machine** (#486 / R34, R57–R58). A round: **serialize dispatches**, run **watched live**, and record a `RoundRecord` pinning the asks dispatched, the worker commit range, the minted patchset generation, and the report board. Wire `runLensPipeline` as the consuming turn — the **round-report drafts FIRST** and gates the regeneration (R58), per-board **arrival events** drive the progressive reveal, and each board's coverage/blemish metadata is persisted through `persistBoardMeta` (reconciliation 5). The rounds ledger is data (`RoundRecord[]`), not UI (C9 out of scope).

## Out of scope

Chat UI (C7), rounds UI (C9), exits **composition** (B11 — B09 pins only the exit-lane ids its own dispatch loop routes on, per `AskSchema.exitLane`'s open string; B11 composes the exits). `SessionModelSchema`, `HarnessCursorSchema`, `ClaimSchema`, `ThreadAnchorSchema`, `AskSchema`, `SessionThreadSchema`, `GenerationSchema`, `RoundRecordSchema` are B03-frozen contracts — consumed, never re-modeled. B08's pipeline pure logic (`core/board/*`, `server/runtime/lens-pipeline.ts`) is consumed, never edited.

## Objective clause → cluster map (every packet clause lands a task)

| Packet Objective clause | Cluster |
| --- | --- |
| Session = first-class durable root (cursor/threads/claim; attach on target-bind; lock once boards exist; new target = new session) | 1 |
| Anchored threads (content transcript-only, boards hold anchor→thread refs) | 1 |
| Harness = cursor-resume; serialize turns per harness id; re-pass options; resume-vanished fallback (boards canonical) | 2 |
| Compaction surfaced honestly (compact_boundary row, PostCompact summary, ask-don't-estimate meter) | 3 |
| Rework = one-shot workers outside the chat, serialized per document | 4 |
| Row click mints session AND claims target; re-entry mid-generation reattaches; pipeline idempotent per session; staging one-path | 5 |
| Rounds loop state machine (serialize dispatches, watched live, round record pins asks/commits/generations, rounds ledger) + wire runLensPipeline (round-report first, arrival→reveal, persistBoardMeta) | 6 |

## Reconciliation ledger (proposer findings — hold these, do not re-open)

1. **The packet's "Existing" file names do not exist — the real seam is `protocol/src/session/model.ts`.** The packet lists *"Existing: `packages/core/src/orchestrator-session*`, `packages/adapters/src/orchestrator-turn.ts`, harness ports"*. Neither `orchestrator-session*` nor `orchestrator-turn.ts` exists in the tree. The actual B09 seam is `packages/protocol/src/session/model.ts` (#466, 2026-08-26), which froze `SessionModelSchema`, `HarnessCursorSchema`, `ClaimSchema`, `ThreadAnchorSchema`, `AskSchema`, `SessionThreadSchema`, `GenerationSchema`, `RoundRecordSchema` — its header assigns *"the state machine, locks, and rework queue"* to B9 explicitly. The nearest existing **turn** analogue is `core/harness-run-turn.ts` (the injected `runTurn`) over `core/harness.ts` (`HarnessPort`); the nearest existing **session** analogue is `server/review-intelligence-session.ts` — but that is an in-memory, per-review flow tracker (`enter()` over a `Map`), **not** the durable root. B09 introduces the durable session runtime; it does not rename or absorb either. (No re-scope: the packet's file pointers were stale; the frozen shapes are the true authority.)
2. **Harness `resume` is "absent by design"; B09 is the slice that adds it.** `core/harness.ts:304` — *"resume/fork are a later slice and are absent by design rather than stubbed."* `HarnessPort` exposes `createSession` only; `SessionSpec` carries no resume field; `claude-query.ts` passes no resume id. The `resume` **capability name already exists** (`CapabilityName`), flagging the intended slice. B09 adds the resume path — a resume field on `SessionSpec` + the adapter wiring in `claude-query` (and the codex adapter's equivalent) — the cursor-resume mechanism the frozen `HarnessCursor` anticipates. This is the machinery the packet Objective assigns to B09 ("Harness = cursor-resume"), not a protocol re-scope; the frozen `HarnessCursorSchema` is consumed unchanged.
3. **`runLensPipeline` is built + tested but has no non-test caller — B09 is its consuming turn.** Confirmed by B08 proposal ledger A1 and `docs/developing/concepts/lens-pipeline.md:33` (*"the consuming turn … is wired by the rounds work (B09)"*). `server/runtime/lens-pipeline.ts` exports `runLensPipeline(deps: LensPipelineDeps)` with the seams left open for B09: `onBoardArrival` (per-board reveal), `persistBoardMeta` (durable coverage/blemishes), `composeTurn` (the orchestrator's heavy authoring turn), `readPrompt`. B09's rounds loop supplies these from the composition root, following the `createKnowledgeSwarmRuntime`/`createProjectScoutRuntime` wiring precedent in `create-server.ts` (~L1311–1325) and the `boardsRuntimeFor` broadcast (~L2037). B09 does **not** modify the pipeline's pure logic.
4. **The staging write path already exists (B04); B09 routes through it.** `server/boards/boards-runtime.ts` (the append choke point) + `projection.ts` + `whiteboard-client.ts` are the one path; B08's host is the sole op writer. The packet's *"staging writes: tool call → event log → projection, one path"* is a **constraint** on B09's rounds loop and session mutations (use the existing path; never a second writer), not a new build. The writer-invariant scan (`whiteboard-client.test.ts`, strengthened in B08 finding A8-adjacent) already guards this; B09 adds its consuming module as a sanctioned caller if and only if it writes board ops directly (it routes through the injected client, so it should not need to).
5. **`persistBoardMeta` durable store is B09's to wire (B08 finding A3 handoff).** B08 left `persistBoardMeta` as an injected seam — *"the composition root supplies the store; B09's consuming turn wires it."* B09 supplies a durable `BoardMeta` store (skippedHunks + validation blemishes) so a result reconstructed after a crash never loses coverage/omission data. The store lands in adapters (file-backed, store precedent) and is injected in `create-server`. The `BoardMeta` shape is B08's (`lens-pipeline.ts` `BoardMeta` interface) — consumed, not re-modeled.
6. **`exitLane` vocabulary: B09 pins only what its dispatch loop routes on; exits composition is B11.** `model.ts:54` — *"the lane id vocabulary locks with B9's state machine, not here."* B09's rounds state machine reads the exit lane the ask carries (`AskSchema.exitLane`, open string) to decide dispatch (the "dispatch round" lane is B09's live path); it does **not** compose the post-review / open-PR exits — that is B11 (out of scope). Reconcile narrowly: B09 defines the lane id(s) its own round dispatch consumes; B11 owns exit composition.
7. **Cursor-resume is wired on the Claude path; the Codex adapter degrades honestly (amendment, cluster 2).** `SessionSpec.resume` is the shared field both adapters accept, and the Claude adapter implements it end to end: `resume` → the SDK `resume` option, and the completed outcome surfaces the harness `session_id` + terminal `uuid` so the durable session persists a real `HarnessCursor`. The Claude adapter therefore advertises the `resume` capability. The **Codex adapter does not** — its app-server `thread/resume` request path and the returned `thread.id` cursor-surfacing cannot be verified offline (the exact method schema is the live binary's, not guessable), and the codex on-disk session-log correlation is documented-gone (`codex-exec.ts:8`). Wiring a durable Codex cursor by guess would be a broken path, not a capability. So Codex keeps `resume` **unimplemented** (capability flag false): a resume spec against Codex simply never surfaces a cursor id, so the turn loop never builds a durable cursor for it and each turn starts fresh — the honest degrade, no gate, no fake. The Codex thread-resume path is a follow-on to be confirmed against the live app-server (cluster 8 E2E territory). This is the mechanism the frozen `HarnessCursor` + T3 design target; the Claude path is that target realized.

## Verification (packet)

`pnpm check` green. E2E: kill the host **mid-generation**, restart, reattach — the session resumes from its persisted cursor, the drafting pipeline does **not** double-start (idempotent per session), and the boards are intact (reconstructed from the event log + persisted `BoardMeta`). A **vanished harness transcript** triggers the honest fallback row ("context rebuilt") while the boards stay canonical. Positive controls that can fail: a second row-click mid-generation that double-starts the pipeline → the idempotency guard fails the test; a persisted cursor whose harness session is gone that silently drops the boards → the resume-vanished fallback assertion fails.

## Completion sigil

`<promise>B09-COMPLETE</promise>`
