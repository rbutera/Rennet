## 1. Spike: prove the WSL interop seam (blocks everything WSL)

- [x] 1.1 On lancelot (hostnames via `tailscale status`; one Windows-native, one WSL), hand-verify the interop primitives: `wsl.exe -d <distro> -- git status` from Windows with piped stdio, stdout encoding checked, and `\\wsl.localhost\<distro>\` reads of a repo file — PASS (see design.md spike findings; distro `Ubuntu`; stdout clean UTF-8/LF; exit codes propagate; UNC reads clean; **argv must use `-e`/`--exec`, not `--`**)
- [x] 1.2 Spike the Claude SDK shim (design decision 2): point `pathToClaudeCodeExecutable` at a `wsl.exe`-based launcher for the distro's `claude` and complete one real streamed turn end to end; record findings (encoding, version probe, arg quoting) in design.md, and if the shim fails, stop and re-open design.md for the in-distro-helper fallback — PRIMITIVE PROVEN (clean stream-json transport end to end; turn blocked only on the distro account being logged out, an env state, not a shim defect); design **not** re-opened

## 2. Locus seam and path handling

- [x] 2.1 Add the `Locus` type (`packages/types`, re-exported from `core`), auto-detected via `detectLocus` from a `\\wsl$`/`\\wsl.localhost` project root; persisted override in `ProjectConfig.locus` + `effectiveLocus`; visible AND editable in project settings via `settings.setRepoLocus` (composition + adapter + IPC + renderer control), plain setting, no ceremony
- [x] 2.2 Implemented `toDistroPath`/`toWindowsView`/`locusCommand`/`detectLocus` as pure node-free functions with unit tests (spaces, non-ASCII, drive letters, both `\\wsl$` and `\\wsl.localhost` prefixes, round-trip). Translation now binds a UNC authority to the expected locus distro and errors with both distro names on mismatch — `packages/core/src/locus.ts`
- [x] 2.3 Routed the injected `git` runners through the locus: `execaGitFor(locus)` (git-range-diff), `GitCaptureAdapter` (dual Windows/distro roots), `GitCheckpointStore` (+ the `GIT_INDEX_FILE` cross-boundary fix via an in-distro `env` prefix), and `repoHasSubmodules`. Composition uses a path-aware runner factory for capture/checkpoint/submit-push plus PR open, project discovery/detail/cleanup, snapshot generation, visibility/settings, and review-side git reads. Snapshot pinning keeps the Windows-view root for host identity/realpath and threads the fixed-locus runner into generation. Uses `-e`/`--exec` per the spike amendment. Argv and dual-root effects are unit-tested.
- [x] 2.4 Audited absolute-path handling: `escape-path.ts` already folds `\`/`:` (added `C:\` + `\\wsl.localhost` regression tests); hardened `resolveWithinRoot` with a pure, separator-injectable `isWithinRoot` (case-insensitive drive letters on win32, UNC roots) + tests. `.rennet/` project keys go through `escapePath` (covered); worktree discovery reads the same escaped keys

## 3. Discovery on Windows and in the distro

- [x] 3.1 Windows host `DiscoveryDeps`: `defaultDiscoveryDeps` is platform-aware — `;`-delimited PATH (`delimiterFor`), no POSIX login-shell harvest on win32 (`loginShellPath` returns null), directly launchable shim matching (`resolveBinaryFilename`: `.cmd`/`.exe`/`.bat`/bare; `.ps1` deliberately excluded), curated Windows dirs (`windowsKnownDirectories`: `%APPDATA%\npm`, `%LOCALAPPDATA%\Programs`, scoop/bun/volta/`.local` per-user). Platform-correct path joins via `joinFor` (a Windows host builds distro POSIX paths with `posix.join`). Unit tests: `harness-discovery.windows.test.ts`
- [x] 3.2 WSL `DiscoveryDeps`: `wslDiscoveryDeps(distro)` harvests the distro PATH (`bash -lc` fixed-literal), lists via the `\\wsl.localhost` UNC view, and probes candidates by executing inside the distro (`wsl.exe … -e`). Every candidate is stamped `locus`; a host binary can never satisfy a WSL project because the composition picks the locus-appropriate deps and the not-found health names the distro. The unused Codex-specific WSL discovery export is removed until Codex-in-WSL lands. Tests cover the distro-found and distro-absent cases.
- [x] 3.3 Locus-aware health: `discoverClaude`/`discoverCodex` emit `unavailable` health whose `detail` names the distro ("…inside the Ubuntu distro"), never a host substitute. This `HarnessHealth` rides the existing health surface; the WSL deps are selected per-project in phase 4 (harness/codex turns), where the distro-named health reaches the review UI

## 4. Harness turns in the WSL locus

- [x] 4.1 Productionised the SDK transport without a script shim: `createClaudeHarness({locus})` discovers the distro Claude and points `pathToClaudeCodeExecutable` directly at `wsl.exe`, using the SDK's installed `executableArgs` API to prepend `-d <distro> --cd <distro-cwd> -e <distro-claude>` as discrete argv. No `.cmd`, PowerShell, shell, or `%*` layer remains; the handoff write turn threads the project locus with native capability. Unit tests capture the SDK's exact spawn argv for spaces, quotes, `%`, `$`, `&`, JSON, and Unicode. REMAINING: a live Windows-side Node proof of this direct SDK spawn chain; the review-pipeline harness sites beyond handoff remain explicitly deferred.
- [ ] 4.2 Codex in WSL — DEFERRED WITHIN CHANGE (honest, per the "record why and continue" latitude). Running `codex exec` in the distro is not a spawn-only change: the executor bakes a HOST scratch dir (`mkdtemp(tmpdir())`) whose `schema.json`/`out.json` a distro-side codex cannot read, and its internal `join(dir,…)` would win32-corrupt distro paths on a Windows host. A correct build needs an executor path refactor (distro scratch via `/mnt/c` mapping OR a distro `/tmp` dir with UNC IO, posix joins, and the session-usage root pointed at the distro `~/.codex/sessions`) plus Windows-node verification I cannot run here. A half-build (spawn-in-distro, host scratch) would BREAK codex on WSL. Left unimplemented so the review honestly degrades to single-Claude on WSL (existing behaviour) rather than shipping a broken codex seat. Design sketch recorded here for the cheap follow-up
- [x] 4.3 Repo watcher: `RepoWatcher.start(root, cb, locus)` uses chokidar polling (`usePolling`, 500ms) for a WSL locus (design decision 7, inotify does not cross 9P/UNC), native event-driven elsewhere; the `.git`/`.rennet` ignore now matches both separator flavours (backslashes on UNC). `startWatching` threads `locusForRepo(root)`. Unit-tested (`repo-watcher.test.ts`)

## 5. Windows-native app surface

- [x] 5.1 Editor resolution on Windows: `open-in-editor.ts` adds `windowsEditorLocations` (per-user `%LOCALAPPDATA%\Programs\…\bin\<cli>.cmd` + Program Files, VS Code/Cursor/Insiders/VSCodium/Sublime) and `.cmd`/`.exe` PATH-shim matching on win32; `editorOpenArgs` launches a WSL-locus open via `--remote wsl+<distro> -g <distro-path>:line` with a `-g` fallback. Resolved shims execute through execa's Windows shim handling rather than raw `execFile`; the `.cmd` effect spec is unit-tested.
- [x] 5.2 Platform-aware keybinding labels: `commands.ts` keybindings are `mod+`-style tokens; `formatKeybinding`/`isMacPlatform` render `⌘X` (macOS) or `Ctrl+X` (Windows/Linux); the palette renders through it; handlers unchanged (still `metaKey || ctrlKey`). Tested
- [x] 5.3 Forge config: `new MakerZIP({}, ["win32"])` added; the app icon selects the Windows `.ico` (`brand/exports/app-icons/windows/rennet-white-on-black`) when packaging on win32; the fuses hook already handles `electron.exe`; `HARNESS_SDK_FILE_EXCLUSIONS` covers `.exe` — asserted by a kept/extended `forge-config.test.ts`
- [x] 5.4 Dev-run on Windows: audited the main process for POSIX-only startup hazards — the only ones (login-shell PATH harvest, editor bundles) are already platform-guarded (discovery returns null loginShellPath on win32; macOS `.app` bundles gated on `darwin`); `node:path`/`homedir` are platform-correct at runtime. The `start`/`package`/`make` targets are cross-platform (Vite + electron). PACKAGING.md gained a Windows section scoping every zsh/`hdiutil`/Apple-signing/DMG step as macOS-only. A live win32 GUI dev-run remains a lancelot manual check (no Windows-side node here)

## 6. Verification on lancelot and docs

- [~] 6.1 Native mode on lancelot — PARTIAL (Windows-node-blocked). Verified headlessly: git-in-distro spawn form, wslpath translation of `C:\`/UNC, discovery PATH harvest + UNC listDir. A live win32 GUI dev-run, a `C:\` project review, and a packaged-ZIP boot need Windows-side node/GUI, which lancelot lacks — listed as manual checks in the PR (design.md phase-6 findings)
- [~] 6.2 WSL mode on lancelot — PARTIAL. Verified headlessly on the real Ubuntu distro: discovery finds the in-distro `git`/`claude`; the direct `wsl.exe -d … --cd … -e claude` transport returns clean stream-json (auth-error only, distro logged out); checkpoint `GIT_INDEX_FILE` plus `git add`/`write-tree` succeeds. The prior `.cmd` proof used `cmd.exe /c` and does not prove the shipped SDK spawn. A live Windows-side Node run of the SDK's direct `wsl.exe` + `executableArgs` chain remains manual, along with a logged-in handoff write/push. Codex and the remaining review-pipeline joins are deferred scope, not manual claims of completion.
- [~] 6.3 Cross-checks — the "host binary never masks a missing distro binary" is enforced by construction (locus-appropriate deps) and the not-found health names the distro (unit-tested, 3.2/3.3). A live stopped-distro status and the host-mask cross-check on the running machine are manual checks (need the GUI)
- [x] 6.4 Docs: added `using/guide/windows-and-wsl.md` (locus concept, native + WSL per-mode requirements, what Rennet never does), linked it in the sidebar + getting-started, and refreshed the getting-started keyboard note. `PACKAGING.md` gained a Windows section (win32 ZIP build, dev run, every zsh/Apple/DMG step scoped macOS-only). `pnpm check` run before push
- [x] 6.5 Filed the Windows release-engineering follow-up as **#330** (signing, installer, auto-update), mirroring #298's Rai-checklist + agent-plan shape, labelled `enhancement,blocked`; explicitly out of this change
