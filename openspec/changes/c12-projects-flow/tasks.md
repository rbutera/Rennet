# Tasks — c12-projects-flow (C12, #489)

Read `openspec/BUILD-LOOP.md` and `context.md` first, then `proposal.md` (its Reconciliations section is part of the spec). One cluster per session; the repo compiles and `sh -c 'pnpm nx affected -t lint,typecheck,test'` is green after every cluster. **One commit per checked task.** Sources of record: INVENTORY §10 (111 `[ws:C12]` claims, `spikes/board-prototype/INVENTORY.md`), the prototype decision (#487), retrieval config (#461), client asset §5 add-project row (#489 comment 5431046569), autopsy S9/S10 + fence (#489 comment 5431046732), spike reference read-only (`spikes/board-prototype/components/{add-project-dialog,add-remote-dialog,directory-browser,project-indexing-view,context-map,new-chat-view,archived-view}.tsx`).

**Session-start bearings (reconciliations 1–3):** C1's `routes/url.ts` already carries every C12 route + builder — fill screens, do not add routes. The reuse targets (`components/directory-browser.tsx`, `context-map/model.ts`, `project/smart-list.ts`) already exist — consume them, close gaps in-place, never re-implement or port the spike copies. `project.discover`/`project.process`/`project.contextMap`/`project.detail` are already declared — bind through the seam with `MemoryBridge` fallback. `session.*` does NOT exist (B9) — cluster 6 is the only live-mint work and is gated on it.

## 1. Add Project dialog + directory browser reuse

- [x] 1.1 `components/add-project-dialog.tsx` (or `project/add-project-dialog.tsx`): the dialog C3's sidebar opens via `ui.openDialog("add-project")`. Source picker (C2 `Popover`/`Command`) listing every environment with its host glyph and a tick on the current one, ending with an **Add Environment** row that dispatches `ui.openDialog("add-environment")`; switching source clears the selected path and reloads the browser against that host. No detected-dirs list, no recents (§10.1). Add inert until a folder is selected; dialog reopens clean each time.
- [x] 1.2 Embed the REUSED `components/directory-browser.tsx` fed by `fs.listDir`, scoped to the selected source's daemon. Do not port the spike's `directory-browser.tsx`. Audit the shipped component against §10.2 claims (breadcrumb inert-current, Up inert-at-root, typed-path Enter, row-descent, arrow/Enter/Backspace, repo badge, dimmed `aria-disabled` unreadable, invalid-path inline alert, "No folders here", stale-listing guard, `listbox`/`option` roving tabindex) and close any gap **in that file**. — AUDIT: shipped component satisfies every §10.2 claim (breadcrumb current-segment `disabled`+`aria-current`; Up `disabled` at root via `parent===null`; path bar navigates on Enter; row click descends; ArrowUp/Down move roving focus, Enter descends, Backspace ascends; `isRepo` "repo" badge; `unreadable` rows `aria-disabled`+opacity-50+non-descendable; failed load shows `role="alert"` + fires `onPathInvalid`; "No folders here" on empty; `generationRef` guard drops stale listings; `listbox`/`option` roles with `tabIndex` roving on `focusIndex`). Zero gaps — embedded as-is, no port.
- [x] 1.3 On Add: run `project.discover` (+ `projects.add`) via `useMutation`, produce no orchestrator turn, and navigate straight to `projectIndexingPath(id)` (§10.1). Sidebar state carries the new project.
- [x] 1.4 DOM tests over `MemoryBridge`: source switch reloads + clears selection; Add inert→enabled on selection; Add navigates to indexing; reopen is clean. Cluster gate green. Commit.

## 2. Add Environment pairing dialog

- [x] 2.1 `components/add-remote-dialog.tsx` opened via `ui.openDialog("add-environment")`: address + one-time code fields (never a bare code); code helper names `rennet pair` and states the printed link fills both fields; Connect inert until both carry a value; fields lock + spinner while connecting; environment name derived from the address's first label; clean reopen each time (§10.3). Reuse the existing host/pairing machinery — no new preload/supervisor work. — Built at `project/add-remote-dialog.tsx` (beside cluster 1's `add-project-dialog.tsx`), mounted in `shell/app-dialogs.tsx`. Binds the real `pairing.exchange` command through the seam (MemoryBridge-backed in tests); the actual remote-address dial + token save is `connection-host.tsx` `submitAdd` shell machinery, deferred as cluster 1 recorded.
- [x] 2.2 On success: name the connected machine, offer Done and **Browse Its Projects** (reopens Add Project with the new environment preselected — one `ui` state hop). DOM tests over `MemoryBridge`: Connect gating, success actions, Browse-Its-Projects preselection. Cluster gate green. Commit. — The `ui` hop is `pendingAddProjectSource` + `openAddProjectForSource(source)` in the `ui` slice; `AddProjectBody` consumes-and-clears it (one-shot, so a later manual reopen is clean). `add-remote-dialog.dom.test.tsx` proves gating, connecting lock+spinner, name derivation, Done, and the end-to-end Browse-Its-Projects preselection (source picker names the paired machine).

## 3. Indexing view — scout phase + questionnaire (against `MemoryBridge` fixtures)

- [x] 3.1 `project/indexing/indexing-view.tsx` behind `/projects/:id/indexing`. Header status ramp scouting → indexing → indexed (§10.4). Scout progression via `project.process`/`project.discover` `onProgress` through `useCommandStream` (fixtures behind `MemoryBridge`; **never the spike's 10.5s `setTimeout`**): read git remotes (name the forge origin), check tracker markers + CI config, read README/CONTRIBUTING/agent files, scout returns stating detected-vs-guessed counts. Deterministic pass before the model-backed seat; generation starts exactly when the scout returns. — View at `project/indexing/indexing-view.tsx`, route wired in `routes/app.tsx` (replaces the interim). Narration rides the REAL `project.process` `onProgress` channel keyed by a per-project stable `commandId` (incumbent `components/project-processing.tsx` pattern), triggered via `useMutation` — the view holds NO timer. Header ramp `data-status` scouting→indexing→indexed; "scout returned" = first `repo-start` (generation begins per-repo). **PACKET-DRIFT:** `project.discover` has NO `onProgress` channel (only `project.process`/`project.detail` do — `data/stream.ts`), and `useCommandStream` folds only into a command's `{repos}` output, so it structurally cannot carry per-stage narration; the scout steps are accumulated from `project.process`'s `onProgress` (the sole real progress channel) — real event shapes, no invented protocol. The deterministic-vs-model scout engine is B7 (out of scope); this is its surface.
- [x] 3.2 Prefilled questionnaire, appearing the instant the scout returns while the map generates beneath it. Heading asks whether prefilled answers look right + states skipping is fine and everything is editable in Settings. Answers: issue tracker, default branch, worktree location, gate command, logo/mark — each with a provenance chip ("detected" neutral / "guessed" machine-register) and a one-line source+use hint. Fixed-option answers as segmented picks; the rest as editable monospace inputs. Escape in a field blurs it, never leaves the view. One action "Looks right" → saved-and-editable confirmation line. Mark is cosmetic, never enters agent context (§10.5). — Landed with the view (same indivisible file, one commit with 3.1). Prefill: default-branch from the REAL `project.primaryBranch` (detected); tracker from a forge named in the scout narration `detail` (detected, else guessed); worktrees/gate/logo are honest "guessed" editable defaults. **PACKET-DRIFT:** the fields beyond default-branch are B7 scout-engine output NOT in `DiscoveryResult` at HEAD (`wire.ts` — it carries only path/kind/repos/primaryBranch/reconciliation/source), so they are surfaced as guessed defaults, never a claimed detection (C12 is B7's surface). Segmented picks use `@rennet/ui` `ToggleGroup`/`Toggle` (NOT hand-rolled `aria-pressed` — `no-handrolled-toggle`, autopsy S6). Escape-in-field `stopPropagation`+blur (the view's window Escape leaves; a field's Escape stops there); "Looks right" collapses to the saved-and-editable line. Mark cosmetic per hint copy.
- [x] 3.3 DOM tests over `MemoryBridge`: scout steps appear/spin/tick in order; questionnaire prefills with correct provenance chips; Escape-in-field blurs not-navigates; "Looks right" collapses to the confirmation line. **Positive control (never-a-gate):** assert the map completes and the exits appear with the questionnaire left unanswered. Cluster gate green. Commit. — `indexing-view.dom.test.tsx`, 5 tests over `MemoryBridge` (the handler captures the view's `project.process` `commandId` so the test drives its `onProgress`). Scout steps appear/spin (only the in-flight step has `.animate-spin`)/tick in DOM order as events are emitted; questionnaire shows exactly 2 "detected" + 3 "guessed" chips with default-branch prefilled from the real project ("trunk") and the "Scout returned · 2 detected · 3 guessed" capstone; Escape on the default-branch field blurs it and the location is unchanged; "Looks right" collapses to the saved line; positive control — `project.process` resolves and both exits navigate (`projectMapPath` / `newChatPath`) with "Looks right" still offered (questionnaire never answered). Full gate below.

## 4. Indexing view — map generation, ready card, Start a Review

- [x] 4.1 Map-generation timeline in the same view, driven by the real `project.process` `onProgress` channel (fixtures behind `MemoryBridge`): steps appear-once-started, spin, tick — scanned working tree (file/scope counts), mapped imports, knowledge workers reading scopes (live N/M + hypothesis count), verifying hypotheses (confirmed/rejected, human never a gate), connected the dots (§10.6). Leaving early (back/Escape) does not cancel; the sidebar row's indexing spinner stays synchronized with the timeline. — The map timeline already rendered the REAL `stage` narration (cluster 3); enriched `narrationLabel` to render `repo-done` as a real "Built <repo> · N files · M symbols" line off the wire summary. Sidebar sync: added `ui.processingProjectIds` (+ `setProjectProcessing` action, `selectProcessingProjectIds`) — the indexing view sets it when it triggers `project.process` and clears it only when the run resolves (leaving never cancels), and `useSidebarTree` unions it into the row's `indexing` flag. **PACKET-DRIFT:** the spike's scripted step labels + counts ("456 files · 12 scopes", live "N/M scopes", in-timeline confirmed/rejected) are fixture text, NOT what `project.process` carries — the real channel emits main-authored per-`stage` `note`+`detail` and a `repo-done` summary (`files`/`symbols`, NO scope or statement counts). The timeline renders the real narration; scope + confirmed/rejected counts are surfaced in the ready card from `project.contextMap` (4.2), their real source.
- [x] 4.2 "Context Map Ready" block on completion (scope/file/confirmed-rejected counts), only action **View Context Map** → `projectMapPath(id)`. Full-width **Start a Review** CTA below it → `newChatPath(id)`, the flow's primary exit, scrolling itself into view at the bottom of a scrolled timeline (§10.7). — Extracted `ReadyCard`, mounted only once `done` so its `project.contextMap` read hits the freshly-built snapshot. File count from the run's own `repo-done`/resolve summaries (real `files`); scope + confirmed/rejected from `project.contextMap` (`map.scopes.length`, `knowledge.statements` by status) — their real source, per 4.1's drift note. An absent map degrades to the file count, never fabricated. View Context Map → `projectMapPath`; the CTA (`newChatPath`) keeps cluster 3's scroll-into-view. Zero new packages.
- [x] 4.3 DOM tests over `MemoryBridge`: steps render in order from the progress channel; sidebar spinner tracks the timeline; leaving does not cancel; ready block shows counts; View Context Map + Start a Review navigate correctly; CTA scroll-into-view fires. Cluster gate green. Commit. — 6 tests added to `indexing-view.dom.test.tsx` (11 total): map steps render in DOM order off the channel (only the newest spins, `repo-done` shows real `456 files · 1200 symbols`); ready block shows `12 scopes · 456 files · 3 confirmed · 1 rejected` from `project.contextMap` + the run summaries; an absent map degrades to the file count with NO fabricated scope/disposition numbers; the CTA fires `scrollIntoView` (spied — happy-dom has none); the sidebar spinner turns on while the run is in flight and off when it resolves (IndexingView + Sidebar mounted together, shared singleton store); and leaving-does-not-cancel — unmounting the view keeps the sidebar spinner on until the run itself resolves. Navigation exits are covered by cluster 3's never-a-gate test. Full gate below.

## 5. Context-map view + `/archived`

- [x] 5.1 `project/context-map-view.tsx` behind `/projects/:id/map`, REUSING `context-map/model.ts` (`buildScopes`/`knowledgeForSelection`/`Selection`) over `project.contextMap`. Base line (repo/ref/object-id + "● current"/"◐ building" chip); structure pane (scopes with importer+file counts, header totals, expand to sample files); dependency-neighborhood SVG (selected scope centered, importers left / imports right, arrowed edges, clickable + keyboard-activatable nodes re-center, edgeless-scope message); Knowledge/Details tabs (statement count; per-statement subject/confidence/status/claim/evidence anchors; proposed → confirm/reject/discuss via `project.knowledgeDisposition`, confirmed/rejected drop controls, rejected dims not disappears, nothing-learned message; Details lists name/root/file/test counts + imports/importers). No ask rail. Leaving the map lands on that project's New Chat (§10.7 context-map). Wire only the entries C12 owns (ready block, new-chat header). — The incumbent `components/context-map-view.tsx` already renders every one of these surfaces over `context-map/model.ts` + `project.contextMap` (base line, "● current"/"◐ knowledge behind map" freshness chip, structure tree, neighborhood SVG with clickable+Enter-activatable nodes, Knowledge/Details tabs, disposition mutation). REUSED it via one additive `showAskRail?: boolean` prop (default true — incumbent test unaffected); the router-side `project/context-map-view.tsx` mounts it with `showAskRail={false}` and `onBack → newChatPath(projectId)`, pulling the bridge from `useBridge`. Zero duplication, zero new packages. **PACKET-NUANCE:** the incumbent's freshness chip reads "● current"/"◐ knowledge behind map" (knowledge-lag honest), not a literal "◐ building" — a still-generating map returns `contextMap` absent (the view mounts only after generation per the ready-card flow), so "building" is the honest absent-reason surface, never a fabricated chip.
- [x] 5.2 `project/archived-view.tsx` behind `/archived`: list archived sessions; restore returns a session to the live sidebar; release is archive-only (§10 archived). Built over `MemoryBridge` (sessions are B9-shaped — no fake session protocol). — Reads the SAME projected tree the sidebar does (`useSidebarTree`), filters `session.archived`, lists rows (target glyph via the shared `TargetIcon`, title, time, project chip); Restore calls the projection's new `restoreSession(id)` — the release is archive-only, no delete. Back/Esc → `newChatPath()`. Sessions stay B9-shaped: the projection is EMPTY in the live client (honest "Nothing archived." until B9), driven via `SidebarSessionProjectionProvider` in tests — no fake session protocol. Added `restoreSession` to `SidebarSessionProjection` + `EMPTY_PROJECTION` (the un-archive half the seam lacked) and to the sidebar/top-bar test projections that type against it.
- [x] 5.3 DOM tests over `MemoryBridge`: map renders scopes/neighborhood from `project.contextMap`; node click re-centers; knowledge disposition fires the mutation and updates state; leaving lands on New Chat; archived list restores a row. Cluster gate green. Commit. — `project/context-map-view.dom.test.tsx` (4 tests): scopes render from `project.contextMap` with the ask rail ABSENT; clicking the `@rennet/ui` SVG node re-centers the neighborhood (aria-label flips); confirm fires `project.knowledgeDisposition` with `{projectId,statementId,disposition}` and the store's confirmed status retires the verbs; Back lands on `newChatPath("p1")`. `project/archived-view.dom.test.tsx` (3 tests): only the archived row lists (the live one is filtered out), Restore un-archives it so it leaves the list (returns to the sidebar — release is archive-only), honest empty "Nothing archived.", Back → `newChatPath()`. Both routes wired in `routes/app.tsx` (map + archived replace their Interims). All 7 green. Full gate below.

## 6. New Chat view — target picker + smart list (surface against `MemoryBridge`)

- [ ] 6.1 `project/new-chat-view.tsx` behind `/new-chat?project=…`: full-view takeover, no chat column; header (back arrow, `project › New Chat` trail, Map control, `esc` hint); Escape closes the page; headline "What should we review in <project>?" with the project name as a headline-sized inline picker (changing it resets the target + updates the URL); composer with a target chip (X → current checkout) and Send inert while empty (§10.8 header/composer).
- [ ] 6.2 Smart list REUSING `project/smart-list.ts` (`buildSmartRows`/`filterSmartRows`/`sortSmartRows`/`smartListCounts`) over `project.detail`: one list no zones; tabs All / Needs you / Mine / Local / PRs with live counts; text filter (PR number/title/branch/repo/author, or branch+repo for local); Escape clears filter first then closes on second press; "Current Checkout · main" pinned first + default target; local rows (mono branch, "● dirty" + accent glyph, note, state chip); PR rows (title, CI dot / failing "CI failing" chip, state chip; second line number/branch/author/±diff/file-count/"checked out locally"); unified target-vocabulary state chips; merged rows dim + lift; repo column drops for single-repo; selected row ticked not striped; empty + filtered-empty copy (§10.8 list).
- [ ] 6.3 DOM tests over `MemoryBridge`: tabs filter + counts; text filter matches the documented fields; Escape two-stage; project-picker change resets target + URL; row-selection ticks; empty states. Cluster gate green. Commit.

## 7. GATED — live session minting (B9) + row-click starts the session

> **Blocked on B9.** No `session.*` command exists at HEAD (reconciliation 3). Run this cluster only when B9's minting commands are on main; otherwise leave the live-mint tasks unchecked with a note — never a hollow pass. All mint logic sits behind one seam module so B9 binds in one place.

- [ ] 7.1 `project/new-chat-mint.ts` — the single seam owning the mint: from a selected row + typed ask, mint a real session (B9 command), derive the title from the target, carry the typed ask as the first turn, claim the target. Bind B9 if present; else expose the same typed surface backed by a `MemoryBridge` mint stub.
- [ ] 7.2 Wire New Chat row click through 7.1 (R26: a click starts the session, not merely a target selection): mint, then navigate into the minted session route; the claimed target's row **vanishes from New Chat**; archiving the session (from `/archived` or the sidebar) reclaims the target so its row reappears (archive-only release). Boundary check only — the minted session's run view (C9) / board (C5) / chat (C7) are out of scope (reconciliation 4).
- [ ] 7.3 DOM/E2E over `MemoryBridge` (whole leg when B9 is live): row click mints + lands on the session route; **positive control** — the claimed row is absent from New Chat after mint and present again after archive. Cluster gate green. Commit.

## 8. Docs (same change, definition of done)

- [ ] 8.1 Update `docs/using/guides/getting-started.md` — the add-project → scout → questionnaire → map → Start a Review journey as it now behaves on the router client (no legacy front-door flow).
- [ ] 8.2 Update `docs/using/guides/context-map.md` — the standalone map view, its entry points, and the never-a-gate questionnaire. Grep `docs/` (excl. `docs/dist`) for any other page describing the old add-project/front-door flow and fix it; record the result even if a no-op.

## 9. Verification (packet)

- [ ] 9.1 `sh -c 'pnpm check'` green — format, architecture, licenses (confirm zero new packages), lint, typecheck, test, build.
- [ ] 9.2 E2E against the real app: add this repo as a project — scout runs, questionnaire prefills with provenance, map generates with live progress, Start a Review mints a session and lands in it (whole leg once B9 is live; boundary check until then); archive + restore from `/archived`. Evidence shown, never asserted; UI proven by driving the real app.
- [ ] 9.3 INVENTORY §10 sweep: every `[ws:C12]` claim spot-checked against the running client; reconciliation-4 claims (835, 839–847 — run-view/board) checked only to the C12 mint-and-land boundary and left to C9/C5/C7 + C14.
- [ ] 9.4 Flip `BUILD-STATUS.json` c12 → `{"status":"done","passes":true}` (or note it left for the landing track per dispatch); emit `<promise>C12-COMPLETE</promise>`.
