# Tasks — c09-rounds (C9, #489)

Read `openspec/BUILD-LOOP.md` and `context.md` first, then `proposal.md` (its Reconciliations section is part of the spec). One cluster per session; the repo compiles and the gate is green after every cluster; one commit per checked task. Sources of record: INVENTORY §7 (~40 `[ws:C9]` claims), the decision #486 (R57/R58) + R34 loop on #458, the client asset §5 rounds row (#489#issuecomment-5431046569), autopsy S9 + fence (#489#issuecomment-5431046732), and the spike reference read-only (`spikes/board-prototype/components/{run-view,round-report}.tsx`). Reused landed surfaces: the C1 data seam (`data/query.ts` `useCommand`, `data/stream.ts` `useCommandStream`), the C1 `run` store slice (`store/run.ts`), C5's board registry + board-data seam (`board/registry.ts`, `board/board-data.ts`, `board/board-view.tsx`, `board/section.tsx`, `board/viewed-delta.ts`), C5's `review` barrel (`review/index.ts` — `RichText`, `AnchorReveal`), the router (`routes/app.tsx`) and its URL grammar (`routes/url.ts` — `ROUTES.sessionRun`, `sessionRunPath`, `VIEW_KINDS` already carries `rounds`). Nothing under `rounds/` imports from `spikes/`. Cluster gate = `sh -c 'pnpm nx affected -t lint,typecheck,test'` unless stated.

**Session-start bearing:** confirm the reused surfaces are on main — `store/run.ts` exports `RunSlice`/`selectRoundRunning`/`selectRunningLaneCount`; `board/registry.ts` exports `ElementRegistry`/`assertNever`/`BOARD_EXCLUDED_KINDS`; `board/board-data.ts` exports `resolveBoard`/`BoardSourceProvider`; `routes/url.ts` exports `ROUTES.sessionRun`/`sessionRunPath` and `VIEW_KINDS` includes `"rounds"`; `routes/app.tsx` mounts an `Interim` at `ROUTES.sessionRun`. Then confirm what stays gated: `grep -rniE "createRounds|rounds\.dispatch|round.?progress|reportBoard" packages/core/src packages/adapters/src packages/server/src packages/protocol/src/commands` — **no rounds command/runtime is registered** (Reconciliation 1), so the live-round binding is the single gated cluster (8). The protocol shapes DO exist: `RoundRecord` (`session/model.ts`), `round_outcome` element (`board/schema.ts`), `SectionDelta` — C9 adds no protocol.

## 1. Foundation — the rounds data seam + the round state machine (Objective: "model run state as a machine", Reconciliations 1/2)

- [x] 1.1 `packages/app-ui/src/rounds/round-machine.ts`: the **pure** run state machine (autopsy S9 fence). A `RoundState` discriminated union — `absent | dispatching | preparing | working | gating | committing | reporting | composing | composed | failed` — carrying only what each phase renders (prep/worker lane rows, progress fraction, the report board id once `reporting`, the new generation id once `composed`). A pure `advance(state, event)` transition (event = a folded `onProgress` payload or a fixture tick), and pure derived predicates the route/greeting read: `canRevealNewBoards(state)` (true only at `composed`), `runNavigation(state, slug)` (the derived target of a terminal transition — the route READS this, it does not compute navigation in an effect). No React, no timers, no `bridge` here.
- [x] 1.2 `packages/app-ui/src/rounds/rounds-data.ts` (mirrors C5's `board/board-data.ts` seam): a `RoundsSource` context resolving three reads for a session — the live `RoundState` (`useRoundState(slug)`), the completed `RoundRecord[]` oldest→newest (`useRoundRecords(slug)`), and a report `LensBoard` by id (`useReportBoard(reportBoardId)`, validated through `resolveBoard`-style parsing — the client never trusts report shape it did not validate). Default context ⇒ **honest-absent** (`state:absent`, no records, `undefined` board). `RoundsSourceProvider` supplies it (fixtures today; deleted/swapped to `useCommand`+`useCommandStream` when B9 lands — cluster 8). Live progress folds into the `RoundState` read via `useCommandStream` the moment a real round-progress channel exists; today the fixture source drives `advance`.
- [x] 1.3 `packages/app-ui/src/test/fixtures/rounds/`: the spike's `run-view`/`round-report` timelines reborn as **fixture data behind the import fence** (a `RoundsSource` that walks `advance` on an injected clock — no `setTimeout` in a rendering path; the tick is a test-driven input). A completed-round fixture (records + a `round_outcome` report board) for the ledger and greeting. Nothing under `rounds/` (outside `test/`) imports these — the fence test stays green.
- [x] 1.4 Unit-test the machine and the seam: `advance` walks `absent→…→composed` on the fixture events; `canRevealNewBoards` is false until `composed`; `runNavigation` yields the new-generation board target ONLY at the terminal transition; the default source is honest-absent; a malformed report board resolves invalid (not a crash, not "no round"). Cluster gate green. Commit.

## 2. The round-report board — reuse and widen C5's registry (Objective: "round report as the greeting (first-class board — reuses the C5 registry)"; INVENTORY §7.1; Reconciliation 3)

- [x] 2.1 `packages/app-ui/src/board/kinds/round-outcome.tsx`: the `round_outcome` renderer — a status pill (`addressed`/`partial`/`untouched`/`beyond` with its tint), the ask reference, the note through `RichText`, and the optional `code_ref` through `AnchorReveal`. One kind, one file (autopsy S4 shape), matching the sibling `board/kinds/` renderers.
- [x] 2.2 `packages/app-ui/src/rounds/report-registry.ts`: the report board's `ElementRegistry` = C5's lens-board renderers **plus** `round_outcome` (the greeting prose reuses the existing `prose` renderer). The report board is a `LensBoard` rendered through C5's `Element`/`Section` machinery over THIS registry — no bespoke report component. Extend the `assertNever` totality so a report registry missing a kind is a **compile error** (task 2.4's positive control). Document at `board/registry.ts`'s `BOARD_EXCLUDED_KINDS` that `round_outcome` is now rendered here (the lens-board exclusion is unchanged — a lens board still rejects it as excluded-kind data).
- [x] 2.3 `packages/app-ui/src/rounds/round-report.tsx`: `RoundReportBoard({ board })` — renders a report `LensBoard` (greeting + `round_outcome` items) through the report registry, plus the status tally line (`N addressed · M partial · …`) derived from the board's items, never stored. This is the shared body the greeting (cluster 5) and the ledger (cluster 6) both mount.
- [x] 2.4 DOM + type tests: a fixture report board renders the greeting and one row per outcome with the right status pill and tally; `AnchorReveal` appears only for an item with a `code_ref`. **Positive control (must be able to fail):** a report registry with a kind omitted fails `typecheck` (the widened `assertNever`), asserted the way C5's registry test asserts totality. Cluster gate green. Commit.

## 3. The run route — live takeover, state machine, deep-linkable cold (Objective clauses: dispatch → live run takeover `/s/:slug/run`, deep-linkable cold, no double-dispatch; autopsy S9; the `setTimeout` rewrite)

- [x] 3.1 `packages/app-ui/src/rounds/run-route.tsx`: `RunRoute({ slug })` reads `useRoundState(slug)` and renders the live run — prep lines, the worker/lens lane list (rows from the machine's state, spinner/queued/done derived, NO `setTimeout`), the gate + commit + "drafting the round report" tail — every label from the folded progress, matching the spike's *shape* but driven by real state. Cold deep-link: on mount it reads current state and (via the seam) reattaches the progress subscription; it **does not dispatch** (dispatch is cluster 4's explicit act) — so a cold mid-round mount reattaches, never double-dispatches. `absent` state (cold link, no live round) ⇒ derive a redirect to the session board via `runNavigation`, not an effect that races itself.
- [x] 3.2 Navigation from transitions (S9 fence): when the machine reaches `reporting`/`composed` the route navigates to `sessionPath(slug, {view:"board"})` (the greeting surface) **derived from `runNavigation(state, slug)`** — one transition function, no `alreadyRanAtMount` ref, no effect reading the state its own `navigate` changes. Reuse the `run` slice for `roundProgress`/`greetingArmed` where the greeting needs continuity across the route change; reset via `resetRun` when a new round starts (not on cold reattach).
- [ ] 3.3 Mount it: in `routes/app.tsx` replace the `Interim` body at `<Route path={ROUTES.sessionRun}>` with `<RunRoute slug={p.slug}/>` (under the existing `RoundsSourceProvider` — cluster 7 wires the provider into the app tree). Wrap the app tree's session subtree in `RoundsSourceProvider` so both the run route and the workspace read one source.
- [ ] 3.4 DOM tests over the fixture source: the run route renders live rows advancing on fixture ticks (no wall clock); a **cold mount at `/s/:slug/run` mid-round reattaches and fires NO dispatch** (the double-dispatch guard — a spy on the seam's `dispatch` records zero calls on mount); reaching `composed` navigates to the board once; `absent` cold-links redirect to the session. Cluster gate green. Commit.

## 4. Dispatch wiring — close C8's `onDispatch` seam (Objective: "dispatch"; C8 left `disabled={!gathering || !onDispatch}`)

- [ ] 4.1 Thread `onDispatch` through the workspace: `app/review-workspace-route.tsx`'s `HandoffMount` supplies `onDispatch` to `HandoffView` (which already forwards to `RoundsLanes`), resolving the rounds seam's `dispatch(slug)` and then `navigate(sessionRunPath(slug))`. Over the default honest-absent source `dispatch` is unavailable ⇒ `onDispatch` absent ⇒ C8's button stays disabled (honest, the truth today); over the fixture/live source it dispatches and takes over the run route. No change to `rounds-lanes.tsx` itself — C8 already gated on `onDispatch` presence.
- [ ] 4.2 DOM test: with a fixture source that can dispatch, staged asks + **Dispatch Round** navigates to `/s/:slug/run` and the run route takes over; with the default absent source the button is disabled (no dead click that lies). Dispatch fires exactly once per click (not on the run route's mount — the cluster-3 guard proves the other half). Cluster gate green. Commit.

## 5. The round report as greeting + progressive reveal (Objective: "round report as the greeting", "progressive reveal (report readable while drafters regenerate beneath; View the New Boards appears at composition, never disabled)"; INVENTORY §7.2)

- [ ] 5.1 `packages/app-ui/src/rounds/round-greeting.tsx`: on return from a round the report board (cluster 2's `RoundReportBoard`) fills the surface, **readable immediately**; beneath it the live regeneration progress (the five lens drafters reworking — rows from the machine's `composing` state, folded `onProgress`, NO `setTimeout`). **View the New Boards** is rendered **iff `canRevealNewBoards(state)`** (i.e. at `composed`) — it appears at composition and is **never a disabled button waiting to enable** (packet + INVENTORY §7.2). Clicking navigates to the new generation's board (`runNavigation`). The retrospective activity-feed line (R32's home) is the settled report's trailing disclosure.
- [ ] 5.2 Wire the greeting into the workspace's board surface: when `useRoundState(slug)` is in a report/regeneration phase, `ReviewWorkspace`'s default (board) view shows the greeting instead of the plain board; once revealed/settled it returns to `LensBoardView` at the new generation. Reuse the `run` slice's `greetingArmed` so the greeting is the first-open affordance and does not re-arm on every render.
- [ ] 5.3 DOM tests: the report is readable while regeneration rows still show "re-drafting"; **View the New Boards is ABSENT before `composed` and PRESENT (never disabled) at `composed`** (both halves asserted — the absent-until + present-never-disabled control); clicking it lands on the new generation's board. Cluster gate green. Commit.

## 6. The rounds ledger (`?view=rounds`) (Objective: "rounds ledger beside Map·Diff (present exactly when a round completed; every report + frozen generation + round diff reachable)"; INVENTORY §7.3)

- [ ] 6.1 `packages/app-ui/src/rounds/rounds-ledger.tsx`: the `?view=rounds` surface — one row per `RoundRecord` (round n, when, the status tally), the selected round's report rendered beneath via `RoundReportBoard` (reuse cluster 2). Each row links what the round record links (INVENTORY §7.3): the report board (`RoundRecord.reportBoard`), the **frozen generation** (`RoundRecord.mintedPatchsetGeneration` / `boardGeneration`) reachable through C5's `GenerationSwitcher`, and the round **diff** (the diff surface at that generation). Verify the #457 append-then-freeze model actually supports reaching an old report + old diff, and record the linkage in the docs (cluster 9).
- [ ] 6.2 The `rounds` view branch + absent-not-disabled pill: add `view === "rounds"` to `ReviewWorkspace` (beside `handoff`/`diff`/default). The `?view=rounds` control is present **exactly when** `useRoundRecords(slug)` is non-empty (the C5-style derived-presence, matching url.ts's "`?view=rounds` gated on a completed round existing") — it sits beside the Map·Diff pill, never a disabled tab. An unknown/absent-round `?view=rounds` falls back to the board (url.ts already falls back on unknown `?view`; add the "no rounds ⇒ fall back" guard).
- [ ] 6.3 DOM tests: with zero rounds the `?view=rounds` pill is **absent** and a direct `?view=rounds` URL falls back to the board; with ≥1 completed round the pill appears, the ledger lists the rounds newest-first, selecting a row renders its report, and the frozen generation is reachable via the generation switcher. Cluster gate green. Commit.

## 7. Post-round delta marks + the rounds barrel (Objective: "post-round delta-mark behavior"; R58; Reconciliation 5)

- [ ] 7.1 Post-round delta marks: on landing at generation N+1, section `delta` badges (`new`/`reworked`; carried = absent, retired via the ask lifecycle) show what changed and **decay on view**, reusing C5's `board/viewed-delta.ts` — do NOT add a new mechanism. Confirm what C5's `section.tsx` already renders (the `LensSection.delta` badge) and add only the post-round *arrival* wiring the C5 board did not: the marks surface when the reviewer arrives at the new generation and decay as sections are viewed. If C5 already draws + decays the badge, this task is a verification + a test that the badge is present on N+1 arrival and gone after view (record the reuse).
- [ ] 7.2 `packages/app-ui/src/rounds/index.ts`: barrel exactly the public rounds surface (the run route, the greeting, the ledger, the `RoundsSourceProvider` + seam types) — trim like C5's barrel, no whole-tree re-export. `app-ui/src/index.ts` re-exports it. Confirm nothing under `rounds/` (outside `test/`) imports from `spikes/` (`grep -rn "spikes/" packages/app-ui/src/rounds` empty) or contains a `setTimeout` in a rendering path (`grep -rn "setTimeout" packages/app-ui/src/rounds` — only test/fixture clock injection, if any).
- [ ] 7.3 DOM test: arriving at generation N+1 shows the delta badges on new/reworked sections; a viewed section's mark decays (reusing the C5 viewed set). Cluster gate green. Commit.

## 8. Gated live cluster — bind to B9's rounds runtime (BLOCKED on B9)

- [ ] 8.1 **Gated on B9.** When B9 registers + binds the rounds runtime (a round-dispatch command, a round-state read, a round-progress push channel, the round-records + report-board reads), swap the seam bodies in `rounds/rounds-data.ts`: `useRoundState` becomes a `useCommand` read with the round-progress channel folded via `useCommandStream`; `useRoundRecords` and `useReportBoard` become `useCommand` reads; `dispatch(slug)` resolves the real round-dispatch command. Mirror C5's `board-data.ts` cluster-8 swap — the seam's callers do not change, only its body; `RoundsSourceProvider` + the fixtures drop out of the app tree (kept for tests). List the exact files that change here when B9 lands. Until then, honest-absent — no fake success.
- [ ] 8.2 **Gated on B9.** Prove the live round end-to-end over the real runtime once bound: a real dispatch drives the run route's live progress, the report arrives as the greeting, the reveal appears at real composition, a real `RoundRecord` appears in the ledger, and a cold deep-link mid-real-round reattaches without double-dispatch. Until B9, this is the fixture-source E2E in cluster 9.

## 9. Packet verification — E2E + positive controls + docs, full gate

- [ ] 9.1 The packet E2E over the fixture rounds source (drives the real UI, not asserted): **dispatch** (staged asks → Dispatch Round → `/s/:slug/run`) → **live progress** (rows advance on injected ticks, no wall clock) → **greeting on return** (report readable while regeneration streams) → **reveal button at composition** (absent-until, present-never-disabled) → **ledger row appears** (`?view=rounds` pill present, the round listed) → **prior generation reachable** (the frozen generation via the switcher) → **cold deep-link to `/s/:slug/run` mid-round reattaches without double-dispatch** (dispatch spy = 0 on mount). Evidence shown.
- [ ] 9.2 Positive controls that can fail (autopsy S9 + registry totality): (a) the report registry totality — an omitted `round_outcome`/kind fails `typecheck` (cluster 2.4); (b) the double-dispatch guard — a cold run-route mount fires zero `dispatch` calls (cluster 3.4); (c) the reveal control — asserted BOTH absent-before-`composed` AND present-never-disabled at `composed` (cluster 5.3); (d) `?view=rounds` — absent with zero rounds, present with ≥1 (cluster 6.3). Confirm each control genuinely fails when its invariant is broken (flip it once, see red, revert).
- [ ] 9.3 Docs (definition of done): update `docs/developing/concepts/handoff-and-exits.md` with the rounds loop end to end (dispatch → live run → report-as-greeting → progressive reveal → ledger → delta marks) and the round-record linkage (report + frozen generation + diff, #457 append-then-freeze verified). Grep `docs/` (excl. `docs/dist`) for pages the change makes wrong (the `app-ui` module map, any page calling the run route / ledger unbuilt) and update or record the grep as a no-op. Do NOT add a "live durable round" claim — that is B9-gated (cluster 8); frame live rounds as planned-Rennet where named.
- [ ] 9.4 Full gate `sh -c 'pnpm check'` green (format, architecture, licenses — confirm **zero new packages** and **zero protocol change**, not assume — lint, typecheck, test, build). Commit. Output the completion sigil `<promise>C09-COMPLETE</promise>` and flip C9's entry in `BUILD-STATUS.json`.
