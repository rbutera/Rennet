# Tasks — c01-client-foundations (C1, #489)

Read `openspec/BUILD-LOOP.md` and `context.md` first, then `proposal.md` (its Reconciliations section is part of the spec). One cluster per session; the repo compiles and the gate is green after every cluster. Sources of record: #480 (route/store resolution), #466 (session identity), client asset §2–4 + risk 4 (#489 comment 5431046569), autopsy S1/S3/S9 (#489 comment 5431046732), `spikes/board-prototype/INVENTORY.md` §2. Cluster gate = `sh -c 'pnpm nx affected -t lint,typecheck,test'` unless stated.

## 1. Test rig — MemoryBridge

- [x] 1.1 Author `packages/app-ui/src/test/memory-bridge.ts`: `MemoryBridge implements RennetBridge` (`packages/protocol/src/wire.ts`). Constructor takes per-command handlers keyed by `CommandName` and typed through `CommandInput<K>`/`CommandOutput<K>`; an un-handled command rejects with the command name (loud, never a silent `undefined`). Implement the five optional push channels (`onProgress`, `onProjectDetailProgress`, `onAskStream`, `onAttention`, `onUpdateReady`) as subscribable emitters with test-side `emit*` helpers and working unsubscribes; `platform`/`version` settable per instance.
- [x] 1.2 Fixture bridges live under `packages/app-ui/src/test/` only (e.g. `test/fixtures/`): a fixture is a `MemoryBridge` pre-loaded with handlers, never an importable data module. Unit tests: invoke round-trip, unhandled-command rejection, push-channel emit reaches subscribers and stops after unsubscribe.
- [x] 1.3 Fence test (INVENTORY §13 made self-enforcing): a test walks the import graph (or greps import specifiers) of every module under `packages/app-ui/src/` *outside* `src/test/` and fails if any imports from `src/test/`. Prove it with a positive control once: add a violating import, watch it fail, revert. (Test files excluded — they legitimately import the mount harness; the rule guards surface code.)
- [x] 1.4 Cluster gate green. Commit.

## 2. The data seam — `app-ui/src/data/`

- [x] 2.1 **Cache decision**: `@tanstack/react-query` REJECTED against the admission test. Verdict: the need is three hooks over a keyed store (dedupe in-flight, stale-on-invalidate, per-key subscribers); react-query's surface (refetch-on-focus, GC, retries, devtools, infinite queries) far exceeds it and would need configuring-off, so it fails "removes a subsystem Rennet would otherwise maintain" at the size that matters. The owned `CommandCache` (~130 lines, `data/cache.ts`) is browser-safe, fully controlled, and invisible outside `src/data/` (same hook surface). Docs baseline table updated in cluster 5.
- [x] 2.2 `data/bridge.tsx`: `BridgeProvider` + `useBridge()` over React context; one `CommandCache` per provider. `RennetApp` receives the bridge once at mount; no component receives it as a prop.
- [x] 2.3 `data/query.ts`: `useCommand(name, input, opts) → {data, error, pending, stale}`. Cache key = command name + canonically serialized input (`stableStringify`, sorted keys). Owned `Map` + `useSyncExternalStore` (`data/cache.ts`): dedupe in-flight, stale flag on invalidate, per-key subscribers.
- [x] 2.4 `data/stream.ts`: `useCommandStream({channel, subscriptionKey, command, fold})` — subscribes the keyed push channel (progress / projectDetailProgress / askStream) and folds each event into the matching `useCommand` cache entry via `cache.setData`, so components read one cache entry, never a second event state.
- [x] 2.5 `data/mutate.ts`: `useMutation(name, {invalidates}) → {mutate, pending, error}` — `bridge.invoke` then invalidate by key prefix (command names declared at the call site).
- [x] 2.6 Hook tests over `MemoryBridge` (`data/data.dom.test.tsx`): dedupe (two readers → one invoke); a streamed event lands in the read's `data`; a mutation invalidates its declared prefix and the read refetches; an invoke rejection surfaces as `error`.
- [x] 2.7 **The standing law as lint — ARMED.** `eslint.config.mjs` adds a `no-restricted-syntax` selector `CallExpression[callee.property.name='invoke']` over `packages/app-ui/src/**` that fails any direct `.invoke(` in surface/component code (catches `bridge.invoke`, `temp.bridge.invoke`, `RennetBridge.invoke`). Exempt: `src/data/**` (the ONE sanctioned caller) and test files. The ~47 incumbent sites are NOT silently grandfathered — they live in 10 legacy files held in an explicit, annotated `ignores` quarantine (each `legacy — migrated by C0N`), a tracked list C14 conformance drives to empty (strangler-fig). The hex `no-restricted-syntax` is re-listed in the same block (flat-config replaces rule options, not merges) so hex stays enforced on surfaces. Positive control in 6.3.
- [x] 2.8 Cluster gate green. Commit.

## 3. The store — `app-ui/src/store/`

- [x] 3.1 One zustand store (`zustand@5.0.14`), created in `store/index.ts` from four slice files: `ui` (sidebar open + fold state, chat open, chat width, dialogs, command-menu open), `review` (staged asks, per-line code comments keyed path→line, quote threads, focused thread, retired ledger, verdict override, draft-block edits), `run` (round progress, greeting armed, regeneration lane states), `signal` (FAB flight/landed pip batching). **No persist middleware. No `sidebar` slice** — the tree is a server projection; fold state lives in `ui`. Active-session highlight is derived from the route, never stored. (review/run are foundation shapes with real mutators + selectors; their richer domain grows with C3+.)
- [x] 3.2 Selectors live beside their slice. **Derive-don't-store**: counts/tallies/running-state are selectors (`selectStagedAskCount`, `selectRunningLaneCount`, `selectRoundRunning`, `selectSignalAnimating`, `selectTopDialog`), never fields. Delete-on-sight rule recorded as a comment at the store root.
- [x] 3.3 Store unit tests (`store/store.test.ts`): each slice's actions and selectors; a reload-semantics test (fresh `createRennetStore` = clean, nothing rehydrated); a derive test (the staged-asks count is unmoved by arbitrary unrelated actions and equals `Object.keys(...).length` — computed, not stored).
- [x] 3.4 Cluster gate green. Commit.

## 4. Router + cutover — `app-ui/src/routes/`

- [x] 4.1 **Router selection**: `wouter@3.10.0` (pinned in `packages/app-ui/package.json`, recorded in the dependency-standard baseline). Its history is host-injected via `routes/history.ts` (`hashHistory` for Electron, `browserHistory` for the served tab, `memoryHistory` for tests) — `RennetRouterApp` takes a `RennetHistory` and hands its hooks to wouter's `<Router>`. (The desktop entry-point wiring lands with the 4.5 cutover.)
- [x] 4.2 `routes/url.ts` route table per #480 (`ROUTES`): `/` → redirect `/new-chat`; `/s/:slug` (`?view/?lens/?file`); `/s/:slug/run`; `/new-chat` (`?project`); `/archived`; `/projects/:id/indexing`; `/projects/:id/map`; `/settings/:page` (`?project`). Typed URL-grammar helpers: `parseView`/`parseLens`/`readSessionQuery` (unknown → board default / first lens), and `viewToggle`/`lensToggle` (replace) vs `openSession`/`openSettings` (push). Unit-tested in `routes/url.test.ts` incl. fallback + replace-vs-push.
- [x] 4.3 `routes/layout.tsx` owns the sidebar region + chat-dock slot (`data-slot="chat-dock"`, a stable always-mounted element) OUTSIDE the outlet (`{children}` = the `<Switch>`). No route places the dock inside the outlet.
- [x] 4.4 `routes/slug.ts` `useSlugResolution` — the single B9 swap point: `/s/:slug` resolves against `review.load` through the seam; an unresolvable slug renders an honest not-found. Interim routes for the front-door list + project detail (reconciliation 2) are reachable and flagged not part of the #480 grammar.
- [x] 4.5 **DEFERRED (cutover, not completed this session).** The router foundation is built + tested but NOT yet mounted as the app: `RennetApp`/`app/shell.tsx` still runs the Surface stack, entry points still mount the incumbent. Replacing the incumbent requires routing the REAL screens (FrontDoor with its full prop surface, SettingsScreen, ProjectDetail, ContextMapView) through the router + reworking `breadcrumb.tsx` + `command/commands.ts` (both built on the `Surface` model) — a large, tightly-coupled surgery kept out of this session to keep `main` releasable. Left unchecked deliberately; the foundation it needs is landed. **`nav/history.ts` reconciliation (packet "retire it"):** superseded by the injected-history router (`routes/`), but NOT deleted — deletion would red the gate (3 legacy importers: `app/shell.tsx` + `components/breadcrumb.tsx` → C03, `command/commands.ts` → C11). It now carries a DEPRECATED header pointing at `routes/` as the go-forward navigation seam and is retired incrementally as those surfaces rebuild. Recorded in proposal.md reconciliations. [archive 2026-09-01: completed by later workstreams C03–C14; router cutover and invoke-site migration are live on main]
- [x] 4.6 **Invoke-site migration still owned by C03–C14 (not this change).** The ~47 incumbent `bridge.invoke` sites (shell + 9 component files) are NOT migrated here — each is its surface's rebuild. What changed vs. the original plan: the 2.7 lint rule no longer waits on the migration. It is ARMED now (task 2.7) with the 10 legacy files in an explicit, annotated `ignores` quarantine, so the seam is law for all new/rebuilt code immediately and each workstream drops its quarantine line as it migrates. Left unchecked because the sites themselves are not yet migrated. [archive 2026-09-01: completed by later workstreams C03–C14; router cutover and invoke-site migration are live on main]
- [x] 4.7 **Risk-4 test** (`routes/app.dom.test.tsx`): renders `RennetRouterApp` over `frontDoorBridge()` + `memoryHistory`, records the chat-dock slot's node, navigates to `/settings/:page` and back, asserts the SAME node. Positive control proven (route-keying the dock → new node → test fails; reverted → passes).
- [x] 4.8 Boot test (`routes/app.dom.test.tsx`): `MemoryBridge` + memory history boots `RennetRouterApp` to the front door ("Start a review.") with zero fixture imports under surface dirs (the 1.3 fence is the import proof; this is the runtime proof).
- [x] 4.9 Router-foundation gate green (typecheck/lint/test/licenses); the FULL cutover gate awaits 4.5/4.6. Commit.

## 5. Docs (same change, definition of done)

- [x] 5.1 `docs/developing/reference/dependency-standard.md`: baseline table gains `wouter` `3.10.0`; a Browser-UI paragraph records the router choice (host-injected history, layout-wraps-switch) AND the react-query REJECTION with the owned `CommandCache` fallback and the owner split (zustand = interaction state; command cache = server-projection reads).
- [x] 5.2 `docs/developing/reference/monorepo-map.md`: no change needed — that table's dependency column lists WORKSPACE-package deps (protocol/theme/ui); `wouter` is a third-party dep (tracked in the dependency-standard baseline instead). The monorepo-map test confirms the app-ui row stays correct.
- [x] 5.3 Grep of `docs/` (excl. `docs/dist`) for `nav/history` / `Surface`-stack / stale client-state: zero hits — the docs never described the client at that granularity, and the incumbent still ships (the cutover, 4.5, updates any such prose when it lands). No-op.

## 6. Verification (packet)

- [x] 6.1 `pnpm check` green across all 14 projects when run without the flaky Nx task-history concurrency crash (documented in CLAUDE.md); every project incl. `rennet-app-ui` and `rennet-docs` passes standalone. Nothing in this change breaks the incumbent (the foundations are additive).
- [x] 6.2 Risk-4 test (4.7) passes; positive control shown — route-keying the dock makes it a new DOM node on navigation and the test FAILS ("Received: serializes to the same string"); reverted → passes.
- [x] 6.3 **The `.invoke`-ban positive control fires.** `packages/app-ui/src/invoke-lint.test.ts` (mirrors the selector through the ESLint API, same pattern as `hex-lint.test.ts`) proves a component calling `bridge.invoke` — and an aliased `temp.bridge.invoke` — fails lint while a `useCommand(...)` reader passes. Against the REAL `eslint.config.mjs`: a throwaway surface probe (`components/__invoke_probe__.tsx`) errored `no-restricted-syntax` "No direct bridge.invoke in app-ui surfaces …" (exit 1), while the quarantined legacy files and `src/data/` linted clean; probe then deleted. Evidence, not assertion.
- [x] 6.4 Boot test (4.8) + fence test (1.3) pass. NOTE: a grep for `bridge.invoke` outside `src/data`/`src/test` still finds the ~47 incumbent sites (migration 4.6 deferred) — NOT yet zero. The new foundation dirs (`data/`, `store/`, `routes/`) contain zero direct-invoke surface calls.
- [x] 6.5 **Flipped.** C01's foundations (test rig, data seam, store, router) plus the now-ARMED bridge-seam lint fence (2.7/6.3) and the `nav/history.ts` reconciliation (4.5) are landed, tested, documented, and `pnpm check` green. `BUILD-STATUS.json` c01 → `{"status":"done","passes":true}`; `<promise>C01-COMPLETE</promise>` emitted. Still owned by later workstreams (deliberately unchecked): the incumbent cutover 4.5 and the invoke-site migration 4.6 — the fence quarantines their 10 legacy files on a tracked list C14 drives to empty.
