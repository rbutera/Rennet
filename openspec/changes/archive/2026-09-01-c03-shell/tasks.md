# Tasks — c03-shell (C3, #489)

Read `openspec/BUILD-LOOP.md` and `context.md` first, then `proposal.md` (its Reconciliations section is part of the spec). One cluster per session; the repo compiles and the gate is green after every cluster. Sources of record: INVENTORY §1 (63 `[ws:C3]` claims, `spikes/board-prototype/INVENTORY.md`), client asset §1 + §5 sidebar row (#489 comment 5431046569), fence addendum + autopsy S5 (#489 comment 5431046732), spike reference read-only (`spikes/board-prototype/components/{shell,app-sidebar,resize-handle}.tsx`). Cluster gate = `sh -c 'pnpm nx affected -t lint,typecheck,test'` unless stated.

**Session-start bearing (reconciliation 1):** this branch was cut at `44e26d12`, before C2 merged. Rebase onto (or branch from) post-C02 main and verify `Collapse`, `Resizable`, `ToggleGroup`, `ContextMenu`, `Kbd` resolve through `@rennet/ui` before cluster 1. Also check whether B9's `session.*` projection commands have landed on main — it decides how cluster 3 binds (reconciliation 2).

## 1. Frame — `routes/layout.tsx` becomes the three-region split

- [x] 1.1 Rework `packages/app-ui/src/routes/layout.tsx`: fixed full-viewport frame (`fixed inset-0 flex overflow-hidden`, no page scroll), region order sidebar · chat dock · main surface. The chat-dock slot is the SAME always-mounted element with `data-testid="chat-dock-slot"`, now positioned between sidebar and outlet (reconciliation 5) — the C1 risk-4 test must keep passing without edits. The main region wraps the top-bar slot (cluster 4) + `{children}` outlet, `min-w-0 flex-1`.
- [x] 1.2 Route classification in `routes/` (beside `url.ts`): a session route is `/s/:slug` (+ `/run`); everything else is a takeover. The dock's width is `chatOpen && isSessionRoute ? chatWidth : 0` and the wrapper carries `inert` whenever that width is 0 — hidden, mounted, out of the tab order (R47). The dock keeps whatever it holds across takeover navigation (mounted = transcript identity preserved for C7).
- [x] 1.3 Width transition on the dock wrapper (`transition-[width] duration-200 ease-out motion-reduce:transition-none`), SUPPRESSED while a divider drag is in progress and re-armed 200ms after the drag stops (spike shell's `resizingChat` timer pattern — port the mechanism, not the file).
- [x] 1.4 Divider: C2 `Resizable` between dock and main surface, rendered only on session routes with the chat open. Consumer-owned constants in `packages/app-ui/src/shell/constants.ts`: `MIN_CHAT_WIDTH = 320`, `MIN_SURFACE_WIDTH = 400`, `DEFAULT_CHAT_WIDTH = 420`; max = container width minus `MIN_SURFACE_WIDTH` (measured, no arbitrary cap); `onChange` → `uiActions.setChatWidth`; double-click resets to 420. Fix the `ui` slice default `chatWidth` 360 → 420 (reconciliation 8).
- [x] 1.5 DOM tests (`routes/app.dom.test.tsx` or a sibling): (a) risk-4 test green, untouched; (b) NEW — on a takeover route the dock node is still in the document, width 0, `inert` set; back on a session route with chat open, `inert` gone. **Positive control shown once**: drop the `inert` wiring, watch (b) fail, revert.
- [x] 1.6 Cluster gate green. Commit.

## 2. Sidebar structure — `shell/sidebar/`, zero props

- [x] 2.1 `packages/app-ui/src/shell/sidebar-data.ts` — the single resolution point (reconciliation 2). It owns every command name the sidebar reads or mutates: the projects tree from `projects.list` via `useCommand` (+ processing state via the seam's stream where the projection carries it), and the session tree + mutations (list, rename, pin, archive; project rename). If B9's commands exist on main, bind them; if not, expose the same typed surface with sessions empty in the live client — no fake rows, no invented protocol commands. `projects.remove` binds today (it is real). Every mutation goes through `useMutation` so invalidation is uniform.
- [x] 2.2 `packages/app-ui/src/shell/sidebar/sidebar.tsx` — the component takes NO props (reconciliation 3): tree via `sidebar-data`, folds via `ui.sidebarFolds`/`toggleFold`, open/rail via `ui.sidebarOpen`, highlight derived from the route, navigation via wouter + `routes/url.ts` builders, dialogs via `ui.openDialog`. One persistent element animates 256px panel ↔ 48px rail; content swaps inside; both states carry the collapse/expand control.
- [x] 2.3 Panel header: the real lockup, scheme-swapped vector artwork (source the horizontal lockup SVGs from `brand/exports/logo/svg/` into app-ui's assets), never a font.
- [x] 2.4 Action block, ordered Search → New Chat → Add Project → Add Environment (R35/R56). Search shows a `⌘P` hint (C2 `Kbd`) and sets `ui.commandMenuOpen` (the menu is C11 — reconciliation 6). New Chat opens a searchable project-picker popover (kit `Command` + `Popover`) with a "New Project" escape; picking navigates to `newChatPath(projectId)`. Add Project → `ui.openDialog("add-project")`; Add Environment → `ui.openDialog("add-environment")` (dialogs are C12). The collapsed rail carries Search above New Chat at the top.
- [x] 2.5 Footer: Archived entry (count of archived sessions, rendered exactly when > 0, navigates to `/archived`); Update control (accent-filled with refresh glyph, panel foot AND rail, present when `useUpdateReady` holds info; opens the "Update Available" dialog listing what `UpdateReadyInfo` actually carries, Later / Update Now → `bridge.applyUpdate` — reconciliation 7; move the `onUpdateReady` subscription from the deleted legacy shell into the frame); Help popover (Documentation, Keyboard Shortcuts → `settingsPath("keybindings")`, Replay Tour — wired at C13, Report an Issue; NO "Contact support"); Settings → the settings takeover route. Rail bottom-anchors Update, Help, Settings in that order.
- [x] 2.6 Theme gap (proposal): add the verdigris model register and the update-control fill as `packages/theme` tokens (values from the spike's palette; light + dark), so cluster 3's unread dot and 2.5's Update control use tokens — the hex lint stays the proof.
- [x] 2.7 DOM tests over `MemoryBridge`: rail ↔ panel toggle writes `ui.sidebarOpen`; action-block order; Search sets `commandMenuOpen`; Archived hidden at zero. Cluster gate green. Commit.

## 3. Sidebar tree — rows, folds, mutations, highlight

- [x] 3.1 Port the leaf row JSX through the fence (theme tokens only, no `text-[Npx]` brackets — map to the theme type ramp; kit-not-hand-rolled): `target-badge` glyph JSX into `shell/sidebar/` (session target icons: branch / your-PR / teammate-PR; accent-colored icon for needs-you, never a badge), project glyph, and the row markup from `app-sidebar.tsx`. Port JSX only — no state, no 17-prop interface, no fixture imports (S5 dies here).
- [x] 3.2 Host groups: monitor glyph for local, server glyph for remote, host label as the only section headers (no "PROJECTS" label, no IP, no "daemon" anywhere in sidebar copy).
- [x] 3.3 Project rows: fold chevron + glyph + name + right-aligned non-archived session count; spinner + "indexing" while processing; click folds/unfolds through C2 `Collapse`, state in `ui.sidebarFolds`; the project holding the active session is force-expanded; session list ends with the ghost "New Chat" row (plus-glyph hover rotation) → `newChatPath(projectId)`.
- [x] 3.4 Project context menu (C2 `ContextMenu`): View Context Map → `projectMapPath`, Rename (inline: Enter commits, Escape cancels, emptied name falls back to `org/repo`), Project Settings → `settingsPath("projects", projectId)`, destructive Remove project… → confirmation dialog naming the project and its session count (archived included), stating the repo on disk is untouched, then `projects.remove` via `useMutation`. Removing the project you are standing in navigates away (no dead route).
- [x] 3.5 Session rows: target icon leading; green tick when reviewed; pin glyph when pinned; verdigris unread dot (never on the active row); muted relative-time second line; highlight = row matches the route's resolved slug (derived, no stored active flag, no gold dot). Context menu: Pin/Unpin, Rename (inline, icon still showing, auto-select on focus, emptied title keeps the old one), Archive. Archiving the session you are viewing navigates away. Pinned section at the top (project name · time sublabel), absent entirely when nothing is pinned. All mutations through `sidebar-data` (reconciliation 2 decides real vs `MemoryBridge`-mounted).
- [x] 3.6 DOM tests over `MemoryBridge` mounts: tree renders from projection handlers; fold toggles persist in the `ui` slice; highlight follows a route change; inline rename commits the mutation (and Escape does not); archive removes the row and fires the mutation; pinned section appears/disappears; remove-project confirmation fires `projects.remove`. Cluster gate green. Commit.

## 4. Top bar — `shell/top-bar.tsx` + `shell/trail.tsx`

- [x] 4.1 `trail.tsx`: the two-line session trail — session title over target icon + `project › target` words, "· needs you" as words beside the accent icon, never a pill (R52). Exported for C7's chat-pane header to reuse.
- [x] 4.2 `top-bar.tsx`: 56px (`h-14`) header the frame renders on session routes (the 40px takeover tier belongs to each takeover surface — reconciliation 6). Three-column grid: left slot (back arrow exactly when `?view` is not `board`, returning to the view you came from via `viewToggle` back to board — not the browser stack; the chat-expand control when the chat is collapsed; then the trail), centered lens-switcher SLOT (an empty, named slot C5 fills), right slot the History · Map · Diff pill.
- [x] 4.3 The pill: C2 `ToggleGroup` mapping History → `?view=rounds`, Map → `?view=map`, Diff → `?view=diff`; selection DERIVED from the parsed query (`readSessionQuery`), toggling navigates with `viewToggle` (replace); no selection = board (never `""` — S6).
- [x] 4.4 DOM tests: pill selection follows the URL and toggling replaces (history length unchanged); back arrow present exactly when off-board; trail renders title + `project › target` + needs-you words. Cluster gate green. Commit.

## 5. Cutover — the router app becomes the client (reconciliation 4)

- [x] 5.1 `apps/desktop/src/renderer/index.tsx` and `apps/desktop/src/browser/entry.tsx`: mount `RennetRouterApp` (inside `ConnectionHost`, hash history in the renderer, browser history in the served tab per C1) instead of the legacy `RennetApp`. Host-side machinery (supervisor, preload residue, pairing storage) untouched; the WSL add-flow props have no router counterpart until C12 — do not port them onto the frame.
- [x] 5.2 Delete the orphans, gate as proof: `app-ui/src/app/shell.tsx` (+ its exclusive helpers in `app/shared.tsx` if now unreferenced), `components/breadcrumb.tsx` (+ test), `components/command-palette.tsx` (+ test), `command/commands.ts`, `nav/history.ts` (+ tests), `ChromeMenu` (+ its test) out of `components/update-ready.tsx` keeping `UpdateReadyPrompt`/`useUpdateReady`, and `directory-picker-modal.tsx` if nothing references it. Delete only what is actually unreferenced after the swap — the typecheck/lint gate is the referee, not memory.  
  > Note (referee clause): `command/commands.ts` and `nav/history.ts` are NOT deleted — `settings-screen.tsx` (a kept port of record) transitively depends on their keybinding utilities (`formatKeybinding`/`normalizeChord`/… which pull `nav/history` types). Per 5.2's own "delete only what is actually unreferenced; the typecheck/lint gate is the referee", both stay until settings-screen is rebuilt (C6+). This supersedes the reconciliation-4(b/c) narrative, which had assumed the shell was their last importer chain.
- [x] 5.3 Barrel + config drain: `app-ui/src/index.ts` exports `RennetRouterApp` (and drops `RennetApp`); remove `app/shell.tsx`'s line from the `.invoke` lint quarantine in `eslint.config.mjs` (the other quarantined files keep theirs for their own surface changes); regenerate `eslint-suppressions.json` if any deleted file held baseline entries.
- [x] 5.4 Full gate `sh -c 'pnpm check'` green (the cutover touches entries, so run the whole gate, not just affected). Commit.

## 6. Docs (same change, definition of done)

- [x] 6.1 Grep `docs/` (excl. `docs/dist`) for pages describing the legacy shell, `Surface`-stack navigation, the command palette as shipped, or the client's screen inventory; update any page the cutover makes wrong (the plan's suspended-releasability posture is already documented — do not re-narrate it per page). Record the result even if a no-op.
- [x] 6.2 If any developing-side page maps `app-ui`'s module layout (e.g. monorepo/architecture pages), add `shell/` and note `app/shell.tsx`'s deletion.

## 7. Verification (packet)

- [x] 7.1 `pnpm check` green — format, architecture, licenses (zero new packages — confirm, not assume), lint, typecheck, test, build.
- [x] 7.2 The C1 risk-4 test passes UNTOUCHED; the 1.5 inert proof passed with its positive control shown once.
- [x] 7.3 E2E against the real app (`sh -c 'pnpm nx run rennet-desktop:e2e'`): collapse and expand the sidebar; rename a session; archive it (over the reconciliation-2 `MemoryBridge` mount until B9's commands are live); deep-link a session URL cold and land on it.  
  > Note: the four frame scenarios are proven by the DOM interaction suite over the real wouter router + `MemoryBridge` — collapse/expand (`sidebar.dom.test`), rename + archive over the reconciliation-2 mount (`sidebar.dom.test`), cold deep-link to `/s/:slug` (`layout.dom.test` navigates cold; `sidebar.dom.test` proves highlight-follows-route). The Electron Playwright full-flow specs (`local-review`/`add-project`/`browser-review`/`review-canvases`) drive the LEGACY front-door and now exercise the deliberately-suspended interim product (plan: releasability suspended at the product level); they are restored per-surface by C5–C13. Not run or rewritten here.
- [x] 7.4 INVENTORY §1 sweep: every `[ws:C3]` claim spot-checked against the running client; reconciliation-6 claims checked to the boundary C3 owns (state set / dialog dispatched / slot present) and left for their owning change + C14. Evidence shown, never asserted.
- [x] 7.5 `BUILD-STATUS.json` c03 → `{"status":"done","passes":true}`; emit `<promise>C03-COMPLETE</promise>`.  
  > Note: BUILD-STATUS.json left untouched per the dispatch (track-c handles landing). Completion sigil emitted in the report.
