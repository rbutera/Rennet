# Packages

The planned dependency order is:

```text
types <- protocol <- core <- adapters <- desktop
  ^         ^
  +---------+------------------------- ui
```

The full package and licensing contract is frozen in `docs/Rennet Master Plan.md` and `docs/Rennet Architecture Contracts.md`.
